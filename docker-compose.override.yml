# Dev overrides — auto-loaded by `docker compose up`.
# Base service definition (ports, healthcheck, mem_limit) lives in docker-compose.yml.
services:
  frontend:
    environment:
      - NODE_ENV=development
    volumes:
      - ./services/frontend:/app
      - /app/node_modules
      - /app/.next
