# docker-compose.override.yml
# Optional dev overrides (disabled by default for production microservices stack).
# To enable local hot-reload development on frontend, uncomment below:
# services:
#   frontend:
#     environment:
#       - NODE_ENV=development
#     volumes:
#       - ./services/frontend:/app
#       - /app/node_modules
#       - /app/.next
