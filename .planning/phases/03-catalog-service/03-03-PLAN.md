---
phase: 03-catalog-service
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - services/catalog-service/scripts/seed.py
  - services/catalog-service/scripts/gen_placeholders.py
  - services/catalog-service/static/products/prod-1001.svg
  - services/catalog-service/static/products/prod-1002.svg
  - services/catalog-service/static/products/prod-1003.svg
  - services/catalog-service/static/products/prod-1004.svg
  - services/catalog-service/static/products/prod-1005.svg
  - services/catalog-service/static/products/prod-1006.svg
  - services/catalog-service/static/products/prod-1007.svg
  - services/catalog-service/static/products/prod-1008.svg
  - services/catalog-service/static/products/prod-1009.svg
  - services/catalog-service/static/products/prod-1010.svg
  - services/catalog-service/static/products/prod-1011.svg
  - services/catalog-service/static/products/prod-1012.svg
  - services/catalog-service/static/products/prod-1013.svg
  - services/catalog-service/static/products/prod-1014.svg
  - services/catalog-service/static/products/prod-1015.svg
  - services/catalog-service/static/products/prod-1016.svg
  - services/catalog-service/static/products/prod-1017.svg
  - services/catalog-service/static/products/prod-1018.svg
  - services/catalog-service/static/products/prod-1019.svg
  - services/catalog-service/static/products/prod-1020.svg
  - services/catalog-service/tests/test_seed.py
autonomous: true
requirements: [CAT-05]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - Running the seed script populates ~20 realistic products with stable string ids prod-1001..prod-1020 and bundled placeholder images
    - Re-running the seed script changes nothing: product count stays ~20 and createdAt is preserved (idempotent upsert)
    - Each seeded product's imageUrl points to /catalog/static/products/{id}.svg and that SVG is served with 200 by the mounted StaticFiles route
  artifacts:
    - services/catalog-service/scripts/seed.py
    - services/catalog-service/static/products/prod-10NN.svg (20 files)
    - services/catalog-service/tests/test_seed.py
  key_links:
    - seed.py imports db.connect/ensure_indexes and the products collection from Plan 01 (no new Mongo connection logic)
    - StaticFiles at /catalog/static was mounted in Plan 01; SVGs committed here are served automatically through the gateway /catalog/** prefix later
  prohibitions:
    - seed must never insert duplicate documents or mutate createdAt on re-run (idempotent upsert with $setOnInsert for createdAt)
    - imageUrl values are root-relative (/catalog/static/...), never absolute external URLs, so they ride the gateway proxy with no extra routing
    - placeholder assets are committed SVGs, not runtime-generated binaries, so re-runs are reproducible
---

<objective>
Populate the catalog with ~20 realistic products and bundle committed placeholder SVG images, idempotently, so re-running the seed is a no-op. This satisfies CAT-05 and gives every later phase (cart, order, frontend) stable product truth (ids, names, prices).

Purpose: A seeded, deterministic catalog is the shared fixture the rest of the platform validates against; idempotency keeps `docker compose down -v && up` reproducible.
</objective>

<execution_context>
@C:/Users/shash/OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\.opencode\gsd-core\workflows\execute-plan.md
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\.planning\phases\03-catalog-service\03-RESEARCH.md
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\.planning\phases\03-catalog-service\03-PATTERNS.md
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform/services/catalog-service/app/db.py
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform/services/catalog-service/app/models.py
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform/docs/api-contracts/catalog-service.openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Idempotent seed script: ~20 products with stable ids, integer priceCents, imageUrl to /catalog/static</name>
  <files>services/catalog-service/scripts/seed.py, services/catalog-service/scripts/gen_placeholders.py</files>
  <read_first>
    - services/catalog-service/app/db.py
    - services/catalog-service/app/models.py
    - .planning/phases/03-catalog-service/03-RESEARCH.md
    - docs/api-contracts/catalog-service.openapi.yaml
  </read_first>
  <acceptance_criteria>
    - seed.py populates exactly 20 products with stable ids prod-1001..prod-1020, integer priceCents, and imageUrl=/catalog/static/products/{id}.svg
    - running the seed twice yields the same count and unchanged createdAt (idempotent upsert via $setOnInsert)
    - gen_placeholders.py generated 20 committed SVGs under static/products
  </acceptance_criteria>
  <action>
    Create `scripts/seed.py` (runnable as `python -m scripts.seed` from the service dir) that imports `db` and calls `await db.connect()` then `await db.ensure_indexes()`. Define a module-level list of ~20 product rows with stable string ids `prod-1001`..`prod-1020`, each carrying `name`, `category` (from a small fixed set like electronics/accessories/apparel/home), `priceCents` (integer, realistic values), `stock` (>=0), optional `description`, and a fixed ISO-8601 `createdAt` string with 3-digit ms + Z. For each row, `imageUrl = "/catalog/static/products/{id}.svg"`.

    Upsert idempotently: `products.update_one({"_id": row["id"]}, {"$set": {k: v for k, v in row.items() if k not in ("id", "createdAt")}, "$setOnInsert": {"createdAt": row["createdAt"], "id": row["id"]}}, upsert=True)`. This preserves `createdAt` and never duplicates on re-run. Print the seeded count at the end.

    Create `scripts/gen_placeholders.py` (optional generator, run once now to produce the 20 SVGs, then commit them) that writes a minimal valid SVG per id into `static/products/prod-NNNN.svg` (a simple rounded-rect with the product name text) — used only to (re)generate assets, not at runtime.
  </action>
  <verify>
    <automated>cd services/catalog-service &amp;&amp; python -m scripts.seed &amp;&amp; python -m scripts.seed &amp;&amp; python -c "import asyncio,sys; sys.path.insert(0,'.'); from app import db; asyncio.run((lambda: (db.connect(), db.products.count_documents({}))))()" 2>/dev/null || docker compose exec -T catalog-service python -c "from app import db; import asyncio; print(asyncio.run(db.products.count_documents({})))"</automated>
  </verify>
  <done>
    seed.py populates exactly 20 products with stable ids prod-1001..prod-1020, integer priceCents, and imageUrl=/catalog/static/products/{id}.svg. Running it twice yields the same count and unchanged createdAt (idempotent). gen_placeholders.py generated the 20 SVG files, which are committed.
  </done>
</task>

<task type="auto">
  <name>Commit 20 placeholder SVGs + test idempotency and static serving (CAT-05)</name>
  <files>services/catalog-service/static/products/prod-1001.svg, services/catalog-service/static/products/prod-1002.svg, services/catalog-service/static/products/prod-1003.svg, services/catalog-service/static/products/prod-1004.svg, services/catalog-service/static/products/prod-1005.svg, services/catalog-service/static/products/prod-1006.svg, services/catalog-service/static/products/prod-1007.svg, services/catalog-service/static/products/prod-1008.svg, services/catalog-service/static/products/prod-1009.svg, services/catalog-service/static/products/prod-1010.svg, services/catalog-service/static/products/prod-1011.svg, services/catalog-service/static/products/prod-1012.svg, services/catalog-service/static/products/prod-1013.svg, services/catalog-service/static/products/prod-1014.svg, services/catalog-service/static/products/prod-1015.svg, services/catalog-service/static/products/prod-1016.svg, services/catalog-service/static/products/prod-1017.svg, services/catalog-service/static/products/prod-1018.svg, services/catalog-service/static/products/prod-1019.svg, services/catalog-service/static/products/prod-1020.svg, services/catalog-service/tests/test_seed.py</files>
  <read_first>
    - services/catalog-service/scripts/seed.py
    - services/catalog-service/app/main.py
    - services/catalog-service/tests/conftest.py
  </read_first>
  <acceptance_criteria>
    - 20 placeholder SVGs are committed under static/products, each a valid standalone SVG served as image/svg+xml
    - test_seed.py is green and proves exactly 20 products, re-seed idempotency (count + createdAt stable), and that GET /catalog/static/products/prod-1001.svg returns 200 with an svg content-type
  </acceptance_criteria>
  <action>
    Ensure the 20 SVG files produced by gen_placeholders.py are committed under `services/catalog-service/static/products/`. Each is a valid standalone SVG (root `<svg xmlns="http://www.w3.org/2000/svg">` element) so it is served with `image/svg+xml` by StaticFiles.

    Create tests/test_seed.py (CAT-05): a test that runs the seed through `db` (call `db.connect()` + `db.ensure_indexes()` in the fixture or test, then `seed.run_seed()` / replicate the upsert logic against the testcontainers mongo), asserts `count_documents({})` == 20, captures one product's `createdAt`, re-seeds, and asserts the count is still 20 and `createdAt` is byte-identical (idempotency). A second test asserts `GET /catalog/static/products/prod-1001.svg` returns 200 with `content-type` containing `svg` (served by the StaticFiles mount established in Plan 01). If exercising the static route via TestClient, mount order must be respected (StaticFiles is mounted at app level in main.py).
  </action>
  <verify>
    <automated>cd services/catalog-service &amp;&amp; pytest tests/test_seed.py -q</automated>
  </verify>
  <done>
    20 placeholder SVGs are committed under static/products; test_seed.py is green proving (1) exactly 20 products, (2) re-seed idempotency (count + createdAt stable), (3) a seeded SVG is served 200 via /catalog/static.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed→MongoDB | trusted one-shot writer; not a runtime request path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering (mass assignment) | seed.py | low | mitigate | Seed sets only known fields; `_id`/`createdAt` handled via upsert; no client input involved |
| T-03-SC | Tampering | pip/uv installs | high | accept | Package Legitimacy Audit (03-RESEARCH.md) cleared all; no action |
</threat_model>

<verification>
- `cd services/catalog-service && pytest tests/test_seed.py -q` green.
- Live: `docker compose up -d mongo catalog-service`, `docker compose exec catalog-service python -m scripts.seed`, then `curl -s -o /dev/null -w '%{http_code}' localhost:8000/catalog/static/products/prod-1001.svg` returns 200, and `curl -s localhost:8000/catalog/products | jq '.total'` equals 20.
</verification>

<success_criteria>
- ~20 realistic products seeded with stable ids and bundled placeholder images (CAT-05).
- Re-running the seed is a no-op (count + createdAt stable).
- Placeholder images are served at /catalog/static/products/{id}.svg.
</success_criteria>

<output>
Create `.planning/phases/03-catalog-service/03-03-SUMMARY.md` when done.
</output>

<section role="flagged-assumptions">
## Flagged Assumptions (Edge Probe — CAT-05 unresolved/unclassified + concurrency)

Surfaced per the specless fallback (never silently dropped):

- **CAT-05 (unclassified):** Seed populates exactly ~20 products (ids prod-1001..prod-1020). Re-running is a no-op (count stable, createdAt preserved) via idempotent upsert. `imageUrl` is root-relative `/catalog/static/products/{id}.svg` (rides the gateway `/catalog/**` proxy; no new gateway route needed in Phase 7).
- **CAT-05 concurrency:** If the seed is invoked concurrently, the per-document upsert on a stable `_id` is idempotent (last-writer-wins per field, createdAt frozen by `$setOnInsert`); concurrent runs cannot create duplicate `_id`s. The canonical usage is a single one-shot run during Compose startup / manual seed, not parallel writers.
</section>

<artifacts>
## Artifacts this phase produces (Plan 03)

- `services/catalog-service/scripts/seed.py` — idempotent seed (run `python -m scripts.seed`)
- `services/catalog-service/scripts/gen_placeholders.py` — one-shot SVG generator (run once, assets committed)
- `services/catalog-service/static/products/prod-1001.svg` … `prod-1020.svg` — 20 committed placeholder SVGs
- `services/catalog-service/tests/test_seed.py` — CAT-05 idempotency + static-serving test
</artifacts>
