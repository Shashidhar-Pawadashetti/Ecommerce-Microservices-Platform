---
phase: 03-catalog-service
plan: 04
type: execute
wave: 3
depends_on: [03-01]
files_modified:
  - services/catalog-service/app/security.py
  - services/catalog-service/app/routes.py
  - services/catalog-service/README.md
  - services/catalog-service/tests/test_admin.py
autonomous: false
requirements: [CAT-06]
user_setup: []
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - POST/PUT/DELETE /catalog/products require a valid HS256 bearer token; missing/invalid/expired/foreign-signed/alg-swapped tokens all return 401 with the byte-identical UNAUTHORIZED envelope
    - Authenticated create returns 201 Product; update returns 200 Product; delete returns 204; unknown id on update/delete returns 404 NOT_FOUND; malformed body returns 400 VALIDATION_FAILED
    - The catalog self-verifies the JWT (iss=ecommerce-auth, aud=ecommerce-api, exp ±60s, alg HS256) using the base64-decoded JWT_SECRET, because the gateway does not exist yet in Phase 3
  artifacts:
    - services/catalog-service/app/security.py (finalized require_auth)
    - services/catalog-service/app/routes.py (createProduct/updateProduct/deleteProduct)
    - services/catalog-service/tests/test_admin.py
  key_links:
    - require_auth (defined Plan 01) is attached as a dependency to every mutating route and declared via openapi_extra security so Swagger shows the lock
    - JWT_SECRET is decoded from base64 exactly like auth-service's signer, or signatures never validate
  prohibitions:
    - mutating routes must never be reachable without a verified JWT (no silent auth bypass; default-deny)
    - the JWT verifier must pin alg HS256 and reject alg=none / RS-HS confusion before signature checks
    - JWT_SECRET value is never logged; only its decoded length is asserted at startup
    - the 401 envelope is byte-identical for every auth failure (no enumeration signal about which case failed)
    - ProductWrite cannot set id or createdAt (mass-assignment guard); server generates them
    - the deviation of catalog holding JWT_SECRET is documented, not hidden
---

<objective>
Implement CAT-06: JWT-protected admin CRUD (create/update/delete) on the catalog, with the catalog self-verifying the HS256 token because the api-gateway (which would normally enforce this) is built in Phase 7. This is the contracted "admin UI" (Swagger + scripted calls, no admin frontend). Refused-without-auth is provable now, pre-gateway.

Purpose: Product truth must be mutable only by authenticated callers; the catalog cannot assume gateway enforcement in Phase 3, so it verifies in-process (defense-in-depth that persists post-Phase-7).
</objective>

<execution_context>
@C:/Users/shash/OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\.opencode\gsd-core\workflows\execute-plan.md
@C:/Users/shash/OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\.opencode\gsd-core\templates\summary.md
</execution_context>

<context>
@C:/Users/shash/OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\.planning\phases\03-catalog-service\03-RESEARCH.md
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\.planning\phases\03-catalog-service\03-PATTERNS.md
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\docs\json-interop.md
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\docs\api-contracts\_shared.yaml
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\services/catalog-service/app/security.py
@C:/Users/shash\OneDrive\Desktop\PROJECTS\Ecommerce-Microservices-Platform\services/catalog-service/app/routes.py
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Confirm catalog becomes a 3rd JWT_SECRET holder (self-verify) in v1</name>
  <files>services/catalog-service/README.md, docs/runbook.md</files>
  <read_first>
    - docs/json-interop.md
    - .planning/phases/03-catalog-service/03-RESEARCH.md
    - .planning/phases/03-catalog-service/03-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - The 3rd-JWT-holder deviation (catalog self-verifies HS256 because the gateway is Phase 7) is explicitly accepted by the user via the blocking checkpoint
    - The decision is recorded in services/catalog-service/README.md (now) and slated for docs/runbook.md (DOCS-02, Phase 10)
    - Self-verify is retained as defense-in-depth even after the gateway exists
  </acceptance_criteria>
  <action>
    Confirm the documented v1 deviation: because the api-gateway is built in Phase 7 but the contract mandates 401 on invalid tokens in Phase 3, the catalog-service MUST hold JWT_SECRET and self-verify HS256 tokens (a 3rd holder beyond the canonical auth-service + api-gateway pair in json-interop.md § Secret Handling). This is a one-way architectural decision — once catalog consumes the secret it cannot be reverted without a code + runbook change. Accept by confirming the deviation is (a) recorded in services/catalog-service/README.md now and (b) carried into docs/runbook.md (DOCS-02, Phase 10) as the canonical deviation note; keep self-verify as defense-in-depth even after the gateway exists.
  </action>
  <verify>
    <human-check>User confirms the 3rd-JWT-holder deviation is accepted and will be documented in README (now) + runbook (Phase 10).</human-check>
  </verify>
  <done>
    The deviation is explicitly accepted and the plan proceeds to implement self-verify + document it. If declined, stop and route back to planning.
  </done>
</task>

<task type="auto" reversibility="one-way" rating="one-way">
  <name>Finalize JWT self-verification in require_auth (base64 decode, HS256 pin, iss/aud/exp ±60s)</name>
  <files>services/catalog-service/app/security.py, services/catalog-service/README.md</files>
  <read_first>
    - services/catalog-service/app/security.py
    - services/catalog-service/app/config.py
    - .planning/phases/03-catalog-service/03-PATTERNS.md
    - docs/json-interop.md
  </read_first>
  <acceptance_criteria>
    - require_auth decodes JWT_SECRET via base64.b64decode before jwt.decode, matching auth-service's signer
    - jwt.decode pins algorithms=["HS256"] and validates audience=ecommerce-api, issuer=ecommerce-auth, exp (±60s leeway)
    - every auth failure (missing/expired/wrong-claims/alg-swap/foreign-signature) raises one byte-identical 401 UNAUTHORIZED envelope
    - startup asserts len(base64.b64decode(JWT_SECRET)) >= 32, logging length only; README documents the deviation
  </acceptance_criteria>
  <action>
    Verify `security.require_auth` (defined in Plan 01) is correct and complete: it must call `jwt.decode(creds.credentials, settings.jwt_secret_bytes, algorithms=["HS256"], audience=settings.jwt_audience, issuer=settings.jwt_issuer)` where `settings.jwt_secret_bytes = base64.b64decode(settings.jwt_secret)` — this MUST match auth-service's signer (auth-service decodes base64 before signing) or no token validates. On any `jwt.PyJWTError` (missing, expired, wrong iss/aud, alg swap, foreign signature) raise `HTTPException(401, detail={"code":"UNAUTHORIZED","message":"Authentication required or credentials invalid."})` — the SAME body for every failure (anti-enumeration). Note PyJWT's default `jwt.decode` applies its own exp leeway (~0s); the contract allows ±60s skew — if stricter leeway is desired, pass `leeway=60`. Keep `config.py`'s startup assertion that `len(base64.b64decode(JWT_SECRET)) >= 32`, logging length only.

    Update services/catalog-service/README.md to record the 3rd-JWT-holder deviation explicitly (why catalog self-verifies pre-gateway, that it remains defense-in-depth post-Phase-7, and that the secret is gitignored and never logged). This is the in-phase documentation of the decision confirmed in the prior checkpoint.
  </action>
  <verify>
    <automated>cd services/catalog-service &amp;&amp; pytest tests/test_models.py tests/test_list.py -q</automated>
  </verify>
  <done>
    require_auth decodes JWT_SECRET from base64, pins HS256, validates iss/aud/exp, and returns one byte-identical 401 envelope for all auth failures. README documents the 3rd-JWT-holder deviation. The verifier is ready to attach to admin routes.
  </done>
</task>

<task type="auto">
  <name>Admin CRUD routes (createProduct/updateProduct/deleteProduct) + 401/400/404 envelopes + tests (CAT-06)</name>
  <files>services/catalog-service/app/routes.py, services/catalog-service/tests/test_admin.py</files>
  <read_first>
    - services/catalog-service/app/routes.py
    - services/catalog-service/app/security.py
    - services/catalog-service/app/models.py
    - services/catalog-service/app/db.py
    - docs/api-contracts/catalog-service.openapi.yaml
    - docs/api-contracts/_shared.yaml
    - .planning/phases/03-catalog-service/03-PATTERNS.md
  </read_first>
  <acceptance_criteria>
    - createProduct/updateProduct/deleteProduct exist, each with dependencies=[Depends(require_auth)] and openapi_extra security bearerAuth
    - missing token -> 401 UNAUTHORIZED; invalid/expired/foreign-signed/alg-swapped token -> 401 same envelope; valid token -> 201/200/204
    - unknown id on update/delete -> 404 NOT_FOUND; malformed body (negative priceCents/empty name) -> 400 VALIDATION_FAILED
    - test_admin.py is green covering all of the above with byte-exact envelope assertions where the contract fixes them
  </acceptance_criteria>
  <action>
    Add three routes to routes.py, all `dependencies=[Depends(require_auth)]` and `openapi_extra={"security": [{"bearerAuth": []}]}` so Swagger shows the lock and `check-contracts.sh` sees the scheme:
    - POST /catalog/products `operation_id="createProduct"` `response_model=Product` `response_model_exclude_none=True`: build doc from `ProductWrite` (`model_dump(exclude_none=True)`), assign `_id` = a new stable-ish string id (e.g. `f"prod-{uuid4().hex[:12]}"` or a monotonic counter — string per Rule 3), set `createdAt` to the current UTC time formatted with the 3-digit-ms serializer, `insert_one`, return the mapped Product with 201. Malformed body (negative priceCents, empty name/category) is rejected by Pydantic with 400 VALIDATION_FAILED via the Pattern 2 handler (no extra work needed).
    - PUT /catalog/products/{id} `operation_id="updateProduct"` `response_model=Product`: if not found -> 404 NOT_FOUND envelope; else `update_one({"_id": id}, {"$set": body.model_dump(exclude_none=True)})` and return the updated mapped Product with 200.
    - DELETE /catalog/products/{id} `operation_id="deleteProduct"`: if not found -> 404 NOT_FOUND; else `delete_one` and return 204 with no body.
    All responses honor `response_model_exclude_none=True`; 400/401/404 bodies come from the custom exception handlers (Pattern 2) and the require_auth dependency — byte-identical to _shared.yaml examples.

    Create tests/test_admin.py (CAT-06) using the `auth_token` fixture from conftest: `test_create_requires_auth` (no token -> 401, body `{"code":"UNAUTHORIZED","message":"Authentication required or credentials invalid."}`); `test_create_valid` (valid token -> 201, returns id/name/priceCents); `test_update_unknown_404` and `test_delete_unknown_404` (valid token, unknown id -> 404 NOT_FOUND envelope); `test_invalid_token_401` (tampered/expired/foreign-signed/alg-swapped token -> 401 same envelope); `test_bad_body_400` (negative priceCents -> 400 VALIDATION_FAILED envelope). Assert envelope bodies byte-exact where the contract fixes them.
  </action>
  <verify>
    <automated>cd services/catalog-service &amp;&amp; pytest tests/test_admin.py -q</automated>
  </verify>
  <done>
    createProduct/updateProduct/deleteProduct exist, require a valid JWT, and return the contracted 201/200/204 (and 400/401/404) envelopes. test_admin.py is green, covering auth refusal (missing + invalid/expired/foreign/alg-swap), successful CRUD, unknown-id 404, and bad-body 400.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→catalog admin API | untrusted bearer token + request body cross here |
| catalog→JWT_SECRET | secret read from env at startup (3rd holder deviation, documented) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation (broken access control) | POST/PUT/DELETE /catalog/products | high | mitigate | `require_auth` dependency on every mutating route; default-deny (public only where `security: []`) |
| T-03-02 | Tampering/Spoofing (JWT alg confusion) | security.py | high | mitigate | `algorithms=["HS256"]` only; base64-decoded secret; validate `iss=ecommerce-auth`, `aud=ecommerce-api`, `exp` ±60s; one byte-identical 401 envelope |
| T-03-04 | Info Disclosure | exception handlers + security | medium | mitigate | Generic envelope; no internals; JWT_SECRET never logged |
| T-03-05 | Tampering (mass assignment) | ProductWrite / admin routes | low | mitigate | ProductWrite excludes id/createdAt; server generates; extra="ignore" |
| T-03-SC | Tampering | pip/uv installs | high | accept | Package Legitimacy Audit (03-RESEARCH.md) cleared all; no action |
</threat_model>

<verification>
- `cd services/catalog-service && pytest tests/test_admin.py -q` green.
- Live: `bash scripts/check-contracts.sh` passes (catalog spec declares operationIds; Spectral clean).
- Manual: `curl -s -o /dev/null -w '%{http_code}' -X POST localhost:8000/catalog/products -H 'content-type: application/json' -d '{"name":"x","category":"c","priceCents":1}'` -> 401; with a valid token -> 201.
- Full suite: `cd services/catalog-service && pytest -q` green across all test modules.
</verification>

<success_criteria>
- Products can be created/updated/deleted only via authenticated API calls; refused without authentication (CAT-06).
- The catalog self-verifies HS256 JWTs (3rd holder, documented), proving CAT-06 pre-gateway.
- 400/401/404 use the shared envelope; Swagger shows admin routes behind bearerAuth.
</success_criteria>

<output>
Create `.planning/phases/03-catalog-service/03-04-SUMMARY.md` when done.
</output>

<section role="flagged-assumptions">
## Flagged Assumptions (Edge Probe — CAT-06 concurrency unresolved)

Surfaced per the specless fallback (never silently dropped):

- **CAT-06 concurrency:** Admin CRUD uses per-document Mongo operations. Concurrent updates to the same `_id` are last-writer-wins (no optimistic-concurrency revision field in v1 — out of scope per contract, which defines no ETag/version). Concurrent creates use distinct generated string ids so they never collide. This is acceptable for v1 admin scripting; if revision conflict handling is later wanted it becomes a new requirement, not a CAT-06 gap.
</section>

<artifacts>
## Artifacts this phase produces (Plan 04)

- `services/catalog-service/app/security.py` — finalized `require_auth` (base64 HS256 self-verify, iss/aud/exp)
- `services/catalog-service/app/routes.py` — createProduct / updateProduct / deleteProduct (admin CRUD)
- `services/catalog-service/README.md` — documents the 3rd-JWT-holder deviation
- `services/catalog-service/tests/test_admin.py` — CAT-06 auth-refusal + CRUD + envelope tests
</artifacts>
