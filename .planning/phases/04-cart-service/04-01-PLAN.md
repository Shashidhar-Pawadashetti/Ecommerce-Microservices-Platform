---
phase: 04-cart-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/cart-service/package.json
  - services/cart-service/Dockerfile
  - services/cart-service/.dockerignore
  - services/cart-service/src/index.js
  - services/cart-service/src/app.js
  - services/cart-service/src/config.js
  - services/cart-service/src/auth/verifyToken.js
  - services/cart-service/src/store/cartStore.js
  - services/cart-service/src/catalog/catalogClient.js
  - services/cart-service/src/totals.js
  - services/cart-service/src/routes/cart.js
  - services/cart-service/src/routes/health.js
  - services/cart-service/src/errors.js
  - services/cart-service/scripts/smoke.js
  - docker-compose.yml
autonomous: true
requirements:
  - CART-01
  - CART-02
gap_closure: false
estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - A logged-in user can POST /cart/items with {productId, quantity} and the server validates the product against Catalog via POST /catalog/products/batch before accepting it.
    - An unknown product id (absent from the catalog batch response) is rejected with code UNKNOWN_PRODUCT and HTTP 404.
    - A quantity below 1 or a missing field is rejected with code VALIDATION_FAILED and HTTP 400.
    - GET /cart returns line and grand totals computed SERVER-SIDE from live Catalog prices; the request body never carries price fields.
    - Cart lines persist in Redis under the key cart:{userId} as a JSON blob.
    - Every cart mutation resets the Redis key TTL (the abandoned-cart expiry anchor).
    - Identity on user-facing operations derives exclusively from the verified JWT sub claim, never from any request body or path field.
    - GET /health returns {"status":"ok"} so Docker Compose can probe liveness.
  artifacts:
    - services/cart-service/package.json (type:module, deps express@5.2.1, ioredis@6.0.0, jsonwebtoken@9.0.3; dev supertest, ioredis-mock)
    - services/cart-service/Dockerfile (node:24-alpine multi-stage, non-root, healthcheck)
    - services/cart-service/src/app.js, src/index.js, src/config.js
    - services/cart-service/src/auth/verifyToken.js (HS256 self-verify, transitional Phase-4 holder)
    - services/cart-service/src/store/cartStore.js (additive add, TTL refresh)
    - services/cart-service/src/catalog/catalogClient.js (batch pricing, no bearer)
    - services/cart-service/src/totals.js (server-side integer-cents totals + CartView builder)
    - services/cart-service/src/routes/cart.js (POST /cart/items + GET /cart), src/routes/health.js
    - services/cart-service/src/errors.js (envelope helper)
    - services/cart-service/scripts/smoke.js (standalone end-to-end smoke)
    - docker-compose.yml (redis:8-alpine + cart-service entries added)
  key_links:
    - verifyToken -> req.user.sub MUST be the only identity source on user-facing ops (T-04-01).
    - catalogClient.batchPricing -> totals MUST use ONLY its response for prices (T-04-02).
    - cartStore.writeCart MUST pass EX (TTL) on every mutation (CART-04 anchor).
    - JWT_SECRET base64-decoded to bytes before jsonwebtoken verify (matches auth signer / catalog verifier).
---

<objective>
Scaffold the Cart Service (Node.js 24 / Express 5.2.1 / ioredis 6.0.0) and prove the end-to-end vertical slice: a JWT-verified add-item call that validates the product against the real Catalog service, followed by a GET /cart that returns server-priced totals read from real Redis. This is the production-quality tracer — every layer (HTTP, auth, catalog client, Redis store, totals) is wired and committed. Standalone smoke verifies it against the running compose stack (real Redis + real Catalog) before expansion plans 04-02..04-04 build out the rest.

Purpose: Establish the platform's first synchronous service-to-service trust boundary and the Redis cart datastore with a proven thin path before layering CRUD and tests.
Output: A runnable cart-service container plus compose wiring (redis + cart-service) and a passing standalone smoke.
</objective>

<execution_context>
@.planning/phases/04-cart-service/04-RESEARCH.md
@docs/api-contracts/cart-service.openapi.yaml
@docs/api-contracts/_shared.yaml
@docs/api-contracts/catalog-service.openapi.yaml
@docs/json-interop.md
</execution_context>

<context>
@.planning/phases/04-cart-service/04-RESEARCH.md
@docs/api-contracts/cart-service.openapi.yaml
@docs/api-contracts/_shared.yaml
@docs/api-contracts/catalog-service.openapi.yaml
@docs/json-interop.md
@services/catalog-service/app/security.py
@services/catalog-service/app/config.py
@services/catalog-service/Dockerfile
@services/auth-service/Dockerfile
@docker-compose.yml
@.env.example
</context>

<tasks>

<task type="tracer">
  <name>Tracer — scaffold cart-service + JWT-verified add→read live-priced totals loop</name>
  <files>services/cart-service/package.json, services/cart-service/Dockerfile, services/cart-service/.dockerignore, services/cart-service/src/index.js, services/cart-service/src/app.js, services/cart-service/src/config.js, services/cart-service/src/auth/verifyToken.js, services/cart-service/src/store/cartStore.js, services/cart-service/src/catalog/catalogClient.js, services/cart-service/src/totals.js, services/cart-service/src/routes/cart.js, services/cart-service/src/routes/health.js, services/cart-service/src/errors.js, services/cart-service/scripts/smoke.js, docker-compose.yml</files>
  <read_first>
    - docs/api-contracts/cart-service.openapi.yaml (paths /cart, /cart/items, /health; schemas AddItem, CartView; threat mitigations T-04-01/T-04-02; decisions D-02/D-04)
    - docs/api-contracts/catalog-service.openapi.yaml (POST /catalog/products/batch: request {productIds}, response omits unknown ids, network-internal/unauthenticated)
    - docs/api-contracts/_shared.yaml (Error envelope {code,message}; Unauthorized/ValidationError responses)
    - docs/json-interop.md (JWT Claims: sub/iss/aud/±60s; integer-cents money; ISO-8601 ms UTC; string ids)
    - services/catalog-service/app/security.py (HS256 verify params: algorithms, audience, issuer, leeway=60)
    - services/catalog-service/app/config.py (base64-decoded secret bytes)
    - services/catalog-service/Dockerfile and services/auth-service/Dockerfile (non-root multi-stage pattern)
    - .planning/phases/04-cart-service/04-RESEARCH.md (Patterns 1-3, Pitfalls 1-8, package pins)
    - docker-compose.yml (entry pattern to extend with redis + cart-service)
  </read_first>
  <action>
    Build the full thin vertical slice as production code (no stubs beyond what is filled later).

    1. package.json: set "type":"module"; scripts dev/start/test; deps express@5.2.1, ioredis@6.0.0, jsonwebtoken@9.0.3; devDeps supertest@7.2.2, ioredis-mock@8.13.1. Run npm install so node_modules exists.
    2. config.js: bind env (all with safe defaults) JWT_SECRET, JWT_ISSUER=ecommerce-auth, JWT_AUDIENCE=ecommerce-api, JWT_TTL_SECONDS=3600, REDIS_URL=redis://redis:6379/0, CATALOG_URL=http://catalog-service:8000, CART_TTL_SECONDS=1209600, PORT=3001. Export a decoded secret getter: Buffer.from(JWT_SECRET,'base64').
    3. errors.js: export sendError(res,status,code,message) that writes compact JSON {"code","message"} only (no internals). Export error classes UnknownProduct, ValidationError, CatalogUnavailable for later plans.
    4. auth/verifyToken.js: middleware verifyBearer. Read Authorization Bearer; if missing/invalid -> sendError 401 UNAUTHORIZED. Else jwt.verify(token, secretBytes, {algorithms:["HS256"], issuer, audience, clockTolerance:60}); attach req.user = decoded; next(). Never read identity from anywhere but req.user.sub. (Phase-4 transitional JWT holder — recorder as a deviation; gateway becomes sole verifier in Phase 7. Reversibility: reversible — env var + middleware removable in Phase 7.)
    5. store/cartStore.js: ioredis client from REDIS_URL. Serialize cart as JSON blob {"items":[{productId,quantity}],"updatedAt":<ISO>}. readCart(userId) -> parsed items (or []). writeCart(userId, items) -> set key with EX CART_TTL_SECONDS (always). addItem(userId, productId, quantity) -> additive: if line exists increment quantity by requested amount, else push; then writeCart. RemoveItem/clearCart to be added in 04-02 but add a no-op-safe stub now is acceptable ONLY as expandable function signatures; do NOT ship incomplete mutation logic that later plans must rewrite — implement addItem fully now and leave updateQty/removeItem/clearCart for 04-02 (different file edits, no architectural change).
    6. catalog/catalogClient.js: async batchPrice(productIds, catalogUrl): POST {productIds} to `${catalogUrl}/catalog/products/batch` with JSON body, NO bearer (network-internal). Use global fetch with AbortController timeout ~2000ms. On non-ok/throw -> throw CatalogUnavailable. Return Map(productId -> {name, priceCents}). Unknown ids are simply absent from the map.
    7. totals.js: buildCartView(userId, items, priceMap): map each line to {productId, name, quantity, unitPriceCents, lineTotalCents = priceCents*quantity}; grandTotalCents = sum. For empty/absent cart return {items:[], grandTotalCents:0, updatedAt: new Date().toISOString(), currency:"USD", userId}. Never read price from request.
    8. routes/cart.js: app.use('/cart', router). POST /cart/items: verifyBearer; destructure ONLY productId (non-empty string) + quantity (integer >=1) from body -> else sendError 400 VALIDATION_FAILED. Call batchPrice([productId]); if !map.has(productId) -> sendError 404 UNKNOWN_PRODUCT. Else addItem then GET priced view: readCart + batchPrice(all ids) + buildCartView; return 200 CartView. GET /cart: verifyBearer; readCart(sub); if empty -> buildCartView empty; else batchPrice(all ids) + buildCartView; 200. (internal /cart/:userId handled in 04-02.)
    9. routes/health.js: GET /health -> 200 {"status":"ok"} (no auth).
    10. app.js: create express app, express.json(), mount health router and cart router, final error handler calling sendError 500 INTERNAL_ERROR (logging only). Export app for supertest. index.js: listen on PORT.
    11. Dockerfile: node:24-alpine multi-stage — deps stage `npm ci --omit=dev` after COPY package*.json; runtime stage copies node_modules + src; create non-root user (addgroup -S app && adduser -r -S -G app app); USER app; EXPOSE 3001; CMD ["node","src/index.js"]. .dockerignore: node_modules, .git, .env.
    12. docker-compose.yml: add `redis` service image redis:8-alpine, NO host port, healthcheck `redis-cli ping | grep PONG`; add `cart-service` service build ./services/cart-service, transitional host port "3001:3001" (revoked in Phase 7 per GTWY-04), mem_limit 256m, env JWT_SECRET/JWT_ISSUER/JWT_AUDIENCE/JWT_TTL_SECONDS/REDIS_URL=redis://redis:6379/0/CATALOG_URL=http://catalog-service:8000/CART_TTL_SECONDS, depends_on redis(healthy)+catalog-service(healthy), healthcheck `wget -qO- http://localhost:3001/health | grep ok || exit 1` (BusyBox grep; interval 15s, timeout 5s, retries 5, start_period 30s).
     13. scripts/smoke.js: mint a JWT with jsonwebtoken (HS256, sub:"smoke-user", iss/aud from env, exp future) using the SAME JWT_SECRET — sign with the BASE64-DECODED secret bytes, i.e. `jwt.sign(payload, Buffer.from(process.env.JWT_SECRET,'base64'), {algorithm:'HS256', ...})`, exactly matching verifyToken.js's decoder, so the signature validates; ensure a known catalog product exists (e.g. prod-1001 seeded by catalog); POST /cart/items {productId:"prod-1001", quantity:2} expecting 200 and grandTotalCents == 2*priceCents; GET /cart expecting matching totals; POST unknown product expecting 404 UNKNOWN_PRODUCT; POST missing token expecting 401. Print PASS/FAIL and exit nonzero on failure.
  </action>
  <verify>
    <automated>docker compose up -d redis cart-service catalog-service mongo && node services/cart-service/scripts/smoke.js</automated>
    <automated>docker compose -f docker-compose.yml config >/dev/null && echo COMPOSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - services/cart-service builds and the container reports healthy (GET /health returns {"status":"ok"}).
    - smoke.js exits 0: a valid-token add of a known product returns 200 with grandTotalCents equal to 2 * catalog priceCents; GET /cart returns matching live-priced totals; an unknown product returns 404 UNKNOWN_PRODUCT; a request without token returns 401.
    - Cart lines are stored in Redis under key cart:{userId} (observable via redis-cli GET cart:smoke-user).
    - The cart blob carries no client-supplied price; only {productId, quantity} are persisted.
    - Identity on the add/read path derives solely from the verified JWT sub (req.user.sub), confirmed by the 401-when-no-token case.
  </acceptance_criteria>
  <done>The tracer slice is committed and the standalone smoke passes against real Redis + real Catalog: add→read returns server-side live-priced totals, unknown product is rejected 404, missing token is rejected 401, and the cart key exists in Redis. The architecture (auth -> store -> catalog -> totals) is proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → cart-service | Untrusted input: bearer token + JSON body over HTTP (transitional host port 3001, revoked Phase 7) |
| cart-service → catalog-service | Network-internal, trusted placement, NO bearer (POST /catalog/products/batch) |
| cart-service → redis | Network-internal, trusted placement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing / Tampering | src/routes/cart.js, src/auth/verifyToken.js | high | mitigate | Identity derived EXCLUSIVELY from verified JWT `sub` (req.user.sub); request bodies carry no identity fields; internal /cart/:userId (added 04-02) stays network-internal and token-gated and is excluded from the gateway route table in Phase 7. |
| T-04-02 | Tampering | src/catalog/catalogClient.js, src/totals.js | high | mitigate | Totals computed solely from the catalog batch response; request `unitPriceCents`/`lineTotalCents`/`grandTotalCents` fields are NEVER read or trusted; integer-cents arithmetic only. |
| T-04-03 | Spoofing | src/auth/verifyToken.js | medium | mitigate | Pin `algorithms:["HS256"]`; verify with base64-decoded secret bytes; validate `iss`/`aud`/`exp` with clockTolerance 60s. Rejects alg-confusion / none swaps. |
| T-04-04 | Info Disclosure | src/errors.js, src/app.js | medium | mitigate | Uniform `{"code","message"}` envelope; messages are human-safe; internals (stack/driver/host) never echoed. |
| T-04-SC | Tampering | npm installs | medium | mitigate | Package legitimacy gate passed: express, ioredis (project-pinned), jsonwebtoken, supertest, ioredis-mock are OK/SUS-as-noted; install exact pinned versions, no postinstall scripts. |
</threat_model>

<verification>
- `docker compose up -d redis cart-service catalog-service mongo` brings cart-service healthy.
- `node services/cart-service/scripts/smoke.js` exits 0 with the assertions in acceptance_criteria.
- `docker compose -f docker-compose.yml config` validates the modified compose file.
</verification>

<success_criteria>
The Cart Service scaffold plus the end-to-end add→read live-priced totals loop is implemented, committed, and verified against real Redis + real Catalog. The first synchronous service-to-service trust boundary (cart→catalog) is proven, and the Redis cart datastore key `cart:{userId}` is established with server-side-only pricing.
</success_criteria>

<output>
Create `.planning/phases/04-cart-service/04-01-SUMMARY.md` when done.
</output>
