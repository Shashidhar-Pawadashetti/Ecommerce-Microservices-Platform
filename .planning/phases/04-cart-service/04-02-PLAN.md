---
phase: 04-cart-service
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - services/cart-service/src/routes/cart.js
  - services/cart-service/src/routes/internal.js
  - services/cart-service/src/errors.js
  - services/cart-service/src/app.js
autonomous: true
requirements:
  - CART-02
  - CART-03
gap_closure: false
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - PATCH /cart/items/:productId sets an ABSOLUTE quantity (PATCH is the sole quantity-setter per D-04); quantity <1 is rejected with VALIDATION_FAILED 400.
    - PATCH on a product that exists in catalog but is not in the cart returns code LINE_NOT_IN_CART 404; a product absent from catalog returns UNKNOWN_PRODUCT 404 (discriminate on Error.code, per OpenAPI 404 examples).
    - DELETE /cart/items/:productId is idempotent: removing an already-absent line still returns 204; UNKNOWN_PRODUCT 404 only when the product is absent from catalog.
    - DELETE /cart empties the cart, idempotent 204, and resets the TTL window.
    - GET /cart/:userId (network-internal checkout read) returns the priced CartView; 404 NOT_FOUND when no live cart exists; it is token-gated and reaches only inside the compose network.
    - All returned totals are computed server-side from live Catalog prices; the client never influences them.
  artifacts:
    - services/cart-service/src/routes/cart.js (expanded: PATCH/DELETE item, DELETE cart)
    - services/cart-service/src/routes/internal.js (GET /cart/:userId)
    - services/cart-service/src/errors.js (error classes UnknownProduct, LineNotInCart, ValidationError, CatalogUnavailable, NotFound)
    - services/cart-service/src/app.js (Express error-handler tail mapping classes to envelope)
  key_links:
    - 404 discrimination order in PATCH/DELETE: catalog batch check BEFORE line-presence check (Pitfall 5 / OpenAPI 404 examples).
    - GET /cart/:userId MUST be excluded from the gateway route table in Phase 7 (network-internal only).
    - Mutations call cartStore.writeCart so the TTL resets (TTL anchor from 04-01).
---

<objective>
Expand the cart API beyond the tracer path: full mutation CRUD (PATCH absolute quantity, DELETE line, DELETE cart) with correct 404 discrimination, plus the network-internal checkout snapshot GET /cart/:userId. All totals remain server-side from live Catalog. This plan owns the route layer only; the Redis store (TTL/persistence) is owned by 04-03, so the two wave-2 plans have no file overlap and run in parallel.

Purpose: Deliver CART-02 (update/remove + server-side totals) and the persistence-facing routes (CART-03 surface), completing the user-facing cart contract.
</objective>

<execution_context>
@.planning/phases/04-cart-service/04-RESEARCH.md
@docs/api-contracts/cart-service.openapi.yaml
@docs/api-contracts/_shared.yaml
</execution_context>

<context>
@.planning/phases/04-cart-service/04-RESEARCH.md
@docs/api-contracts/cart-service.openapi.yaml
@docs/api-contracts/_shared.yaml
@services/cart-service/src/routes/cart.js
@services/cart-service/src/store/cartStore.js
@services/cart-service/src/catalog/catalogClient.js
@services/cart-service/src/totals.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full cart mutation CRUD with 404 discrimination</name>
  <files>services/cart-service/src/routes/cart.js, services/cart-service/src/errors.js, services/cart-service/src/app.js</files>
  <read_first>
    - docs/api-contracts/cart-service.openapi.yaml (PATCH /cart/items/{productId}, DELETE /cart/items/{productId}, DELETE /cart; 404 examples unknownProduct vs lineNotInCart)
    - docs/api-contracts/_shared.yaml (ValidationError, Unauthorized, NotFound envelopes)
    - .planning/phases/04-cart-service/04-RESEARCH.md (Pitfall 5: 404 discrimination; D-04 delete-only quantity-zero rejection)
    - services/cart-service/src/routes/cart.js (04-01 tracer router to extend)
    - services/cart-service/src/store/cartStore.js (addItem/updateQty/removeItem/clearCart signatures)
    - services/cart-service/src/catalog/catalogClient.js (batchPrice returning Map)
    - services/cart-service/src/totals.js (buildCartView)
  </read_first>
  <action>
    Extend routes/cart.js (mounted at /cart) WITHOUT rewriting the tracer POST/GET already there:
    - PATCH /cart/items/:productId: verifyBearer; destructure ONLY quantity from body; if not integer >=1 -> sendError 400 VALIDATION_FAILED (D-04: zero/negative rejected; removal is DELETE-only). Call batchPrice([productId]); if !map.has(productId) -> sendError 404 UNKNOWN_PRODUCT. Else readCart(sub); if no line for productId -> sendError 404 LINE_NOT_IN_CART. Else updateQty(sub, productId, quantity) (absolute), then return 200 buildCartView over readCart+priceMap.
    - DELETE /cart/items/:productId: verifyBearer; batchPrice([productId]); if !map.has -> sendError 404 UNKNOWN_PRODUCT. Else removeItem(sub, productId) which is idempotent (no error when line missing); return 204 (no body).
    - DELETE /cart: verifyBearer; clearCart(sub) (idempotent); return 204.
    Add error classes to errors.js: UnknownProduct, LineNotInCart, ValidationError, CatalogUnavailable (already referenced in 04-01), plus NotFound. In app.js append an Express error-handler tail (4-arg) that maps: UnknownProduct->404 UNKNOWN_PRODUCT "Product not found.", LineNotInCart->404 LINE_NOT_IN_CART "Cart line not found.", ValidationError->400 VALIDATION_FAILED, CatalogUnavailable->503 SERVICE_UNAVAILABLE "Catalog unavailable.", NotFound->404 NOT_FOUND, else 500 INTERNAL_ERROR. Log the raw error server-side only; never echo internals to the client (T-04-04).
    Do NOT modify cartStore.js here (owned by 04-03). If updateQty/removeItem/clearCart are missing, implement them in 04-03's store task; this plan calls them by contract.
  </action>
  <verify>
    <automated>MISSING — 04-04 creates tests/test_update_remove.js and tests/test_add.js; interim verify via `docker compose up -d redis cart-service catalog-service mongo` then curl: PATCH known line qty=3 -> 200 grandTotal=3*price; PATCH missing line -> 404 LINE_NOT_IN_CART; PATCH unknown product -> 404 UNKNOWN_PRODUCT; DELETE line -> 204; DELETE again -> 204; DELETE /cart -> 204.</automated>
  </verify>
  <acceptance_criteria>
    - PATCH an existing line with quantity q returns 200 and grandTotalCents == q * catalog priceCents.
    - PATCH with quantity 0 or missing returns 400 VALIDATION_FAILED.
    - PATCH a product not in cart (but in catalog) returns 404 LINE_NOT_IN_CART; PATCH a product not in catalog returns 404 UNKNOWN_PRODUCT (codes discriminated, not just status).
    - DELETE /cart/items/:productId returns 204 on first and repeated calls (idempotent); DELETE /cart returns 204 even when empty.
    - No error response leaks stack traces, hostnames, or driver internals.
  </acceptance_criteria>
  <done>Mutation CRUD complete with correct 404 discrimination and uniform envelopes; all totals read from the catalog response. The route layer is the single owner of HTTP semantics for these endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Network-internal checkout snapshot GET /cart/:userId</name>
  <files>services/cart-service/src/routes/internal.js, services/cart-service/src/app.js</files>
  <read_first>
    - docs/api-contracts/cart-service.openapi.yaml (GET /cart/{userId}: internal, token-gated, 404 when no live cart; D-02 singular path spelling)
    - .planning/phases/04-cart-service/04-RESEARCH.md (Architecture: order-service calls with its own token; excluded from gateway in Phase 7)
    - services/cart-service/src/auth/verifyToken.js (verifyBearer middleware)
    - services/cart-service/src/store/cartStore.js (readCart)
    - services/cart-service/src/catalog/catalogClient.js (batchPrice)
    - services/cart-service/src/totals.js (buildCartView)
  </read_first>
  <action>
    Create routes/internal.js exporting an Express router mounted at '/' (so the full path is /cart/:userId, literal singular per D-02). Define GET /cart/:userId:
    - verifyBearer (the caller — order-service — presents its OWN valid bearer; this is the single sanctioned exception to sub-derived identity, reachable only inside the compose network).
    - readCart(userId); if empty/absent -> sendError 404 NOT_FOUND (NOT UNKNOWN_PRODUCT — this is a missing cart, not a missing product).
    - Else batchPrice(ids) + buildCartView(userId, items, priceMap); return 200 CartView.
    Register the router in app.js (app.use(internalRouter)). It stays outside the gateway route table in Phase 7 (documented; not enforced here).
  </action>
  <verify>
    <automated>04-04 creates tests/test_internal.js asserting 200 (populated cart, valid token), 404 NOT_FOUND (no live cart), and 401 (no token) for GET /cart/:userId; interim verify via curl with a valid minted token: GET /cart/smoke-user with a populated cart -> 200 CartView; GET /cart/nonexistent-user -> 404 NOT_FOUND; GET without token -> 401.</automated>
  </verify>
  <acceptance_criteria>
    - GET /cart/:userId with a populated cart and a valid token returns 200 with the priced CartView (totals server-side).
    - GET /cart/:userId for a user with no live cart returns 404 NOT_FOUND.
    - GET /cart/:userId without a token returns 401.
    - The path is the singular /cart/:userId (not /carts/...), matching contract decision D-02.
  </acceptance_criteria>
  <done>Internal checkout read implemented and token-gated; returns priced snapshot or 404 NOT_FOUND for absent carts. Surface complete for CART-02/CART-03 route coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → cart-service | Untrusted bearer + JSON body (transitional host port, revoked Phase 7) |
| order-service → cart-service (/cart/:userId) | Network-internal, token-gated, NOT routed through gateway in Phase 7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing / Elevation | src/routes/internal.js, src/routes/cart.js | high | mitigate | User-facing ops use req.user.sub only. GET /cart/:userId is the sole path-derived-id exception, stays network-internal, token-gated, and is excluded from the gateway in Phase 7. No client-supplied userId honored on user-facing POST/GET/PATCH/DELETE. |
| T-04-02 | Tampering | src/routes/cart.js, src/totals.js | high | mitigate | PATCH/DELETE still recompute totals from catalog batch response; request never carries price. Integer-cents only. |
| T-04-04 | Info Disclosure | src/app.js error handler | medium | mitigate | All error classes mapped to uniform {code,message}; raw errors logged server-side only. |
| T-04-SC | Tampering | npm installs | medium | mitigate | Pinned approved versions; no new packages introduced by this plan. |
</threat_model>

<verification>
- Mutation endpoints verified manually against the running stack (interim) and by 04-04 automated tests.
- Internal GET /cart/:userId verified for 200 / 404 / 401 cases.
- No error envelope leaks internals.
</verification>

<success_criteria>
CART-02 and the persistence-facing route surface are delivered: full update/remove/clear with correct 404 discrimination and server-side totals, plus the internal checkout snapshot. The route layer is the sole owner of these HTTP semantics; the store behind it is hardened in 04-03.
</success_criteria>

<output>
Create `.planning/phases/04-cart-service/04-02-SUMMARY.md` when done.
</output>
