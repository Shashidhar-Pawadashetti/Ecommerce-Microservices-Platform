---
phase: 04-cart-service
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - services/cart-service/package.json
  - services/cart-service/tests/conftest.js
  - services/cart-service/tests/test_auth.js
  - services/cart-service/tests/test_add.js
  - services/cart-service/tests/test_update_remove.js
  - services/cart-service/tests/test_totals.js
  - services/cart-service/tests/test_persist.js
  - services/cart-service/tests/test_ttl.js
  - services/cart-service/tests/test_internal.js
autonomous: true
requirements:
  - CART-01
  - CART-02
  - CART-03
  - CART-04
gap_closure: false
estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Every user-facing route returns 401 without or with a bad/invalid token (T-04-01 anti-enumeration: one envelope).
    - Adding an unknown product returns UNKNOWN_PRODUCT 404; invalid/missing quantity returns VALIDATION_FAILED 400 (CART-01).
    - PATCH of a non-present line returns LINE_NOT_IN_CART 404; DELETE is idempotent 204 (CART-02).
    - Returned line and grand totals equal quantity * live catalog priceCents aggregated; the client cannot influence totals (T-04-02 / CART-02).
    - After an add, the cart key cart:{userId} exists in Redis (CART-03).
    - With a tiny CART_TTL_SECONDS, after a mutation the TTL resets and after the expiry window elapses the key is gone / GET /cart returns empty items (CART-04 observable).
  artifacts:
    - services/cart-service/package.json (test script "node --test")
    - services/cart-service/tests/conftest.js (app factory + redis client injection)
    - services/cart-service/tests/test_auth.js, test_add.js, test_update_remove.js, test_totals.js, test_persist.js, test_ttl.js, test_internal.js
  key_links:
    - test_ttl MUST use REAL redis (ioredis-mock eviction unreliable per research A5); tiny TTL; assert key gone after window.
    - Logic tests use ioredis-mock to stay container-free; only TTL test needs real redis.
---

<objective>
Build the automated test suite (node:test + supertest) that proves every CART requirement: auth enforcement, add validation, update/remove semantics, server-side totals correctness, persistence, and observable TTL expiry. The TTL test runs against real Redis (ioredis-mock eviction is unreliable — research Assumption A5); logic tests use ioredis-mock so the default `npm test` stays container-free except the TTL file.

Purpose: Provide the Nyquist gate for all four CART requirements and a green phase-exit signal.
</objective>

<execution_context>
@.planning/phases/04-cart-service/04-RESEARCH.md
@docs/api-contracts/cart-service.openapi.yaml
@docs/api-contracts/_shared.yaml
</execution_context>

<context>
@.planning/phases/04-cart-service/04-RESEARCH.md
@docs/api-contracts/cart-service.openapi.yaml
@services/cart-service/src/app.js
@services/cart-service/src/store/cartStore.js
@services/cart-service/src/catalog/catalogClient.js
@services/cart-service/src/totals.js
@services/cart-service/src/auth/verifyToken.js
@services/catalog-service/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test harness + auth/add/update/totals/persist tests (node:test + supertest)</name>
  <files>services/cart-service/package.json, services/cart-service/tests/conftest.js, services/cart-service/tests/test_auth.js, services/cart-service/tests/test_add.js, services/cart-service/tests/test_update_remove.js, services/cart-service/tests/test_totals.js, services/cart-service/tests/test_persist.js</files>
  <read_first>
    - .planning/phases/04-cart-service/04-RESEARCH.md (Validation Architecture test map; Pitfalls 4-7; T-04-01/T-04-02)
    - docs/api-contracts/cart-service.openapi.yaml (all response codes + Error.code values)
    - services/cart-service/src/app.js (exported app for supertest)
    - services/cart-service/src/store/cartStore.js (readCart/readTtl signatures)
    - services/catalog-service/tests/conftest.py (testcontainer/mock pattern to mirror in JS)
  </read_first>
  <action>
    Add "test": "node --test" to package.json scripts (keep dev/start). Create tests/conftest.js that imports the exported `app` from src/app.js and builds a supertest agent; it obtains a Redis client for assertions: for logic tests use ioredis-mock pointed at a fake URL so cartStore hits in-memory redis (inject via a module-level setter or by setting REDIS_URL to a mock before importing the store); for the TTL test use a REAL redis URL from env (REDIS_URL) when RUN_REDIS_TTL=1. Provide a helper to mint a valid HS256 JWT (jsonwebtoken, same JWT_SECRET, iss/aud, future exp) and an invalid/expired one for 401 cases. Seed a fake catalog: override catalogClient.batchPrice via dependency injection or by pointing CATALOG_URL at a local nock/lightweight stub returning [{productId:"prod-1001",name:"Keyboard",priceCents:12999}] for known ids and omitting unknown ids (mirrors the contract's omission behavior) — do NOT require the real catalog for logic tests.
    Write:
    - test_auth.js: GET/POST/PATCH/DELETE /cart* without token -> 401 UNAUTHORIZED (one envelope); with expired/invalid token -> 401.
    - test_add.js (CART-01): POST /cart/items {productId:"prod-1001",quantity:2} -> 200 and grandTotalCents==25998; unknown id -> 404 UNKNOWN_PRODUCT; quantity:0 and missing productId -> 400 VALIDATION_FAILED.
    - test_update_remove.js (CART-02): PATCH existing line qty=3 -> 200 grandTotal==3*price; PATCH qty=0 -> 400; PATCH not-in-cart -> 404 LINE_NOT_IN_CART; PATCH unknown -> 404 UNKNOWN_PRODUCT; DELETE line -> 204 then GET shows it gone; DELETE again -> 204 (idempotent); DELETE /cart -> 204.
    - test_totals.js (CART-02/T-04-02): assert returned unitPriceCents/lineTotalCents/grandTotalCents equal catalog values and are unaffected by any extra body fields; assert integer-cents math; assert empty cart grandTotal 0.
    - test_persist.js (CART-03): after POST add, readCart(sub) via store helper returns the line; redis key cart:{sub} exists (ioredis-mock supports keys/get).
    - test_internal.js (internal GET /cart/:userId, CART-03): with a populated cart + valid token -> 200 returning a priced CartView (items with unitPriceCents/lineTotalCents, grandTotalCents); with no live cart for the target userId -> 404 NOT_FOUND; without a token -> 401 UNAUTHORIZED. This endpoint is the network-internal checkout read consumed by order-service in Phase 5; it MUST be verified here (resolves plan-checker blocker).
  </action>
  <verify>
    <automated>cd services/cart-service && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (logic files) passes: 401 on every protected route without token; UNKNOWN_PRODUCT/VALIDATION_FAILED on bad add; LINE_NOT_IN_CART vs UNKNOWN_PRODUCT discriminated; DELETE idempotent 204; totals equal quantity*priceCents and ignore any client price fields; cart persisted under cart:{userId}; internal GET /cart/:userId returns 200 priced CartView when populated, 404 when no live cart, and 401 without a token.
    - No test exercises real Redis unless RUN_REDIS_TTL=1 (TTL file skipped otherwise).
  </acceptance_criteria>
  <done>Logic test suite green and covering CART-01/CART-02/CART-03 plus auth/T-04-01/T-04-02; default run is container-free.</done>
</task>

<task type="auto">
  <name>Task 2: Observable TTL expiry test (CART-04) against real Redis + smoke note</name>
  <files>services/cart-service/tests/test_ttl.js, services/cart-service/README.md</files>
  <read_first>
    - .planning/phases/04-cart-service/04-RESEARCH.md (Pitfall 2; Assumption A5 ioredis-mock unreliable for TTL; Wave 0 Gaps TTL observability)
    - docs/api-contracts/cart-service.openapi.yaml (TTL touched on every mutation; updatedAt anchor)
    - services/cart-service/src/store/cartStore.js (readTtl, writeCart EX behavior)
    - services/cart-service/README.md (04-03 note to extend)
  </read_first>
  <action>
    Create tests/test_ttl.js guarded so it only runs when RUN_REDIS_TTL=1 (skip with a clear message otherwise, to keep default `npm test` green without Docker). When enabled: point the store at REAL redis (REDIS_URL env, e.g. redis://localhost:6379/1), set CART_TTL_SECONDS to a tiny value (e.g. 2) via env or by calling a store setter; POST add a known product; assert cart:{userId} key exists and readTtl(userId) > 0 and is near CART_TTL_SECONDS; perform a second mutation (PATCH qty) and assert readTtl increased/reset (TTL anchor refreshed); then `await sleep(CART_TTL_SECONDS*1000 + 500)` and assert readTtl returns -2 (key gone) AND GET /cart returns items:[] (abandoned cart expired). This proves CART-04 observably.
    Extend README.md with a "Testing" section: `npm test` runs container-free logic tests; `RUN_REDIS_TTL=1 REDIS_URL=redis://localhost:6379/1 npm test` runs the TTL test against real Redis; note ioredis-mock eviction is unreliable so the TTL test must use real Redis.
  </action>
  <verify>
    <automated>cd services/cart-service && RUN_REDIS_TTL=1 REDIS_URL=redis://localhost:6379/1 node --test tests/test_ttl.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - With RUN_REDIS_TTL=1 the TTL test passes: key exists after add with positive TTL; TTL resets after a mutation; after the window the key is gone and GET /cart returns empty items.
    - Without the flag the test is skipped (default `npm test` stays green and container-free).
    - README documents how to run the TTL test against real Redis and why real Redis is required.
  </acceptance_criteria>
  <done>CART-04 is proven observably: abandoned carts expire after the configurable window and the test asserts the key's disappearance; documented run instructions included.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → cart-service (supertest) | In-process HTTP, no real network |
| test_ttl → real redis | Local-only, requires explicit RUN_REDIS_TTL=1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Spoofing | tests/test_auth.js | high | mitigate (test-asserted) | Tests assert 401 on missing/invalid/expired tokens for every protected route; verifies identity is never trusted from the body. |
| T-04-02 | Tampering | tests/test_totals.js | high | mitigate (test-asserted) | Tests assert totals equal catalog-derived priceCents and ignore injected body price fields. |
| T-04-SC | Tampering | npm installs | medium | mitigate | supertest@7.2.2 / ioredis-mock@8.13.1 are legitimacy-gated OK; pinned. |
</threat_model>

<verification>
- `cd services/cart-service && npm test` exits 0 (logic suite; TTL skipped by default).
- `RUN_REDIS_TTL=1 REDIS_URL=redis://localhost:6379/1 node --test tests/test_ttl.js` passes when real Redis is available.
</verification>

<success_criteria>
All four CART requirements have automated coverage: CART-01 (add + validation), CART-02 (update/remove + server-side totals), CART-03 (persistence key), CART-04 (observable TTL expiry). The suite is the phase-exit gate and runs container-free except the explicitly-flagged TTL test.
</success_criteria>

<output>
Create `.planning/phases/04-cart-service/04-04-SUMMARY.md` when done.
</output>
