---
phase: 01-contracts-repo-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - scripts/check-contracts.sh
  - scripts/validate-topic-schemas.mjs
  - docs/api-contracts/.spectral.yaml
  - docs/api-contracts/_shared.yaml
  - docs/api-contracts/auth-service.openapi.yaml
autonomous: false
requirements: [CONTR-01, CONTR-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "bash scripts/check-contracts.sh mechanically validates all five CONTR dimensions (lint, topic schemas, LF law, manifest completeness, endpoint coverage) and exits non-zero on any violation"
    - "The auth-service contract is complete enough that Phase 2 can implement signup/login/me without asking a single question — every operation has request/response schemas, error refs, and explicit security classification"
    - "JWT conventions are frozen contractually: HS256 pinned by name, iss=ecommerce-auth, aud=ecommerce-api, claims sub/email/roles/iat/exp, TTL 3600s ±60s skew, secret = CSPRNG ≥32 bytes held by auth+gateway only"
    - "The tracer slice proves the whole phase chain end-to-end BEFORE remaining specs are authored: shared components → service spec → lint → gate green"
  artifacts:
    - "scripts/check-contracts.sh (5-stage gate, existence-guarded for incremental waves)"
    - "scripts/validate-topic-schemas.mjs (Node stdlib fenced-JSON validator; no jq — jq is absent on this machine)"
    - "docs/api-contracts/.spectral.yaml (extends spectral:oas)"
    - "docs/api-contracts/_shared.yaml (bearerAuth scheme, Error envelope {code,message}, Unauthorized/NotFound/Conflict responses, IdempotencyKey + pagination parameters)"
    - "docs/api-contracts/auth-service.openapi.yaml (OpenAPI 3.0.3; signup/login/me; explicit no-logout note per D-03)"
  key_links:
    - "auth-service.openapi.yaml $refs ./_shared.yaml#/components/* — Spectral dereferences fully, so a broken ref aborts the lint loudly (desired failure mode)"
    - "check-contracts.sh stage-1 lints the glob docs/api-contracts/*.openapi.yaml so every later spec plan inherits validation automatically"
    - "_shared.yaml bearerFormat comment points to docs/json-interop.md as canonical JWT claims table (authored in Plan 03)"
  prohibitions:
    - statement: "No float or decimal money representation anywhere in schemas or examples — integer-cents fields only"
      status: forbidden
      verification: "grep -nE 'type: (number|float)' docs/api-contracts/*.yaml produces no matches inside money fields; example values end in Cents-named integer fields"
    - statement: "No generated spec artifacts committed (nothing matching *.openapi.json tracked)"
      status: forbidden
      verification: "git ls-files | grep -i 'openapi.json' outputs nothing"
    - statement: "No ambiguity hedges in contract descriptions — descriptions never say probably/maybe/TBD-style wording"
      status: forbidden
      verification: "grep -inE '\\b(TBD|probably|maybe)\\b' docs/api-contracts/*.yaml outputs nothing"
---

<objective>
Build the mechanical drift gate and prove it end-to-end on ONE complete contract (the auth spec) before authoring the remaining three specs — the phase's tracer slice.

Purpose: Phase 1's entire value is enforceability ("does reality match the contract?" must be a command, not a conversation). This plan makes the command exist, then feeds it a real contract through every stage.

Output: working `check-contracts.sh` + topic-schema validator + Spectral ruleset; `_shared.yaml` shared components; complete `auth-service.openapi.yaml`; full gate exit 0.

Locked decisions implemented here:
- **D-03**: Logout has NO endpoint — auth spec carries an explicit informational note that logout is client-side token discard by design (v1), so no later phase helpfully adds one
- **D-JWT**: HS256 pinned on issue AND verify sides; issuer/audience/claims/TTL frozen as truths above (canonical table lands in Plan 03's json-interop.md; this spec summarizes + links)
</objective>

<execution_context>
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Standard Stack, §Package Legitimacy Audit, §Validation Architecture, §Code Examples, §Contract Content Inventory auth table, §Security Domain)
@.planning/phases/01-contracts-repo-scaffolding/01-VALIDATION.md
</context>

<precondition>Node.js >= 20 with npx available on PATH (research verified v24.18.0 / npm 11.16.0 present); jq is ABSENT — validators use node only.</precondition>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — @stoplight/spectral-cli@6.16.3 before first install/run</name>
  <files>.planning/phases/01-contracts-repo-scaffolding/01-02-SUMMARY.md</files>
  <read_first>.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Package Legitimacy Audit — SUS verdict rationale and escape hatch)</read_first>
  <what-built>Registry audit of the linting dependency this phase runs via npx: @stoplight/spectral-cli pinned at 6.16.3 — flagged SUS ("too-new" freshness flag on the latest publish only)</what-built>
  <how-to-verify>
    Before ANY npx invocation of this package, verify legitimacy at https://www.npmjs.com/package/@stoplight/spectral-cli and https://github.com/stoplightio/spectral:
    1. Weekly downloads approximately 1.66M (long-established project)
    2. Official stoplightio repository, not archived/deprecated
    3. No install/postinstall scripts on the package page
    4. Research evidence says the flag is a release-freshness false positive
    Options: APPROVE npx usage (recommended); FALLBACK to official Docker image stoplight/spectral; DEFER lint automation to Phase 2 keeping only the script skeleton (contracts do not depend on the linter being installed). Record chosen path in SUMMARY; Tasks 2–3 adapt their verify commands to it.
  </how-to-verify>
  <resume-signal>Type "approved" (npx), "docker", or "defer" — or describe concerns</resume-signal>
  <verify>SUMMARY.md records the legitimacy verdict and chosen execution path (npx | docker | defer).</verify>
  <done>Explicit human approval for the flagged package exists before first use; fallback path documented if rejected.</done>
  <acceptance_criteria>SUMMARY names the package, the verification URLs checked, and the selected option.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Validation gate trio — check-contracts.sh + validate-topic-schemas.mjs + .spectral.yaml</name>
  <files>scripts/check-contracts.sh, scripts/validate-topic-schemas.mjs, docs/api-contracts/.spectral.yaml</files>
  <read_first>.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Validation Architecture Wave 0 Gaps, §Code Examples check-contracts.sh skeleton + validate-topic-schemas.mjs core), .planning/phases/01-contracts-repo-scaffolding/01-VALIDATION.md (Wave 0 Requirements)</read_first>
  <action><!-- planner-discipline-allow: jq --> Write scripts/check-contracts.sh with set -euo pipefail and five stages, each accumulating FAIL=1 rather than aborting early:
  (1) LINT — if docs/api-contracts/*.openapi.yaml glob is non-empty, run the Spectral CLI pinned at 6.16.3 via npx against that glob with ruleset docs/api-contracts/.spectral.yaml and --fail-severity=warn (adapt to Docker image or skip-with-note per Task 1 decision);
  (2) TOPIC SCHEMAS — node scripts/validate-topic-schemas.mjs docs/kafka-topics.md, exiting 0 with a SKIP note while kafka-topics.md does not yet exist (authored Plan 05), failing on real content violations once it does;
  (3) EOL LAW — git ls-files --eol piped through grep i/crlf excluding *.bat and *.cmd endings must output NOTHING;
  (4) MANIFEST — each required-pin grep runs ONLY when docs/versions.md exists (authored in parallel by Plan 03): assert Spring Boot 3.5.16, Spring Cloud 2025.0.3, JDK 21, FastAPI 0.141.1, Pydantic 2.13.4, Python 3.13, Node 24, Express 5.2.1, ioredis 6.0.0, kafkajs 2.2.4, nodemailer 9.0.5, aiokafka 0.14.0, pymongo 4., apache/kafka 4.2.1, postgres 18, mongo 8.0, redis 8, Next.js 16.3.2, Mailpit, eclipse-temurin 21-jre, plus a verified-date footer line. This existence-guarded SKIP is BY DESIGN, not a defect: Plan 03 authors the manifest in this same wave under parallel execution, so at wave start the file legitimately does not exist yet; the FULL assertion set is exercised when Plans 04 and 05 invoke the gate after Plan 03 completes;
  (5) SPEC COVERAGE — per-spec op-count greps guarded on each file's existence (counts finalized in Plans 04/05; this stage asserts auth spec contains operations signup, login, me from Plan 03 onward).
  Write scripts/validate-topic-schemas.mjs (~60 lines, Node stdlib ONLY): read the markdown file, extract fenced json blocks, map them to topics order.created and payment.completed, JSON.parse each (unparseable = fail), assert required-key sets exactly [eventId, orderId, userId, userEmail, items, totalCents, currency, createdAt] and [eventId, orderId, outcome, reason, processedAt] respectively, assert outcome ∈ APPROVED|DECLINED, assert items elements carry productId/nameSnapshot/unitPriceCents/quantity, assert totalCents parses as integer (Number.isInteger after extraction) — NEVER use jq anywhere.
  Write docs/api-contracts/.spectral.yaml: extends: ['spectral:oas']; add rule requiring info.description non-empty on each spec (severity warn). All three files executable/committed.</action>
  <verify><automated>bash scripts/check-contracts.sh exits 0 immediately after creation (partial state: zero specs authored yet → stages SKIP gracefully, EOL stage passes post-Plan-01)</automated></verify>
  <done>The gate exists, is idempotent, tolerates incremental wave states, and fails loudly on genuine violations.</done>
  <acceptance_criteria>`bash scripts/check-contracts.sh` exit code 0 on the partial tree; deliberately appending a fenced block missing eventId to a temp copy of a topics file makes validate-topic-schemas.mjs exit non-zero; `grep -cE "spectral-cli@6\.16\.3" scripts/check-contracts.sh` >= 1 and `grep -c "jq" scripts/check-contracts.sh` == 0; .spectral.yaml contains `spectral:oas`.</acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3 (TRACER): _shared.yaml + complete auth-service.openapi.yaml validated end-to-end by the gate</name>
  <files>docs/api-contracts/_shared.yaml, docs/api-contracts/auth-service.openapi.yaml</files>
  <read_first>.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Pattern 1 authority header, §Pattern 2 shared components + gotchas, §Code Examples Error envelope, §Contract Content Inventory auth table, §Security Domain V2/V3/V6 rows, Pitfall 4 schema-encoding rules)</read_first>
  <action><!-- planner-discipline-allow: operationId --> <!-- planner-discipline-allow: bearerAuth --> <!-- planner-discipline-allow: logout --> _shared.yaml: top-of-file authority comment (Source of truth: THIS FILE set; never commit generated specs); openapi 3.0.3; info block; components.securitySchemes.bearerAuth = http bearer with bearerFormat JWT (comment: HS256 pinned; canonical claims table lives in docs/json-interop.md); components.schemas.Error = object required [code,message], code described as machine-readable stable identifier (examples VALIDATION_FAILED, DUPLICATE_EMAIL), message forbidden from leaking internals (no hostnames/stack traces/driver errors), additionalProperties true (ignore-unknown reader policy); reusable responses Unauthorized (401), NotFound (404), Conflict (409), ValidationError (400) all $ref-ing Error; parameter IdempotencyKey = header string required=true per ORDR-05; pagination parameters limit (integer, default 20, maximum 100) and offset (integer, default 0).
  auth-service.openapi.yaml: same authority header; title Auth Service API, version 1.0.0; servers entry noting gateway prefix /auth (port mapping documented Phase 7); info.description carrying the JWT conventions summary AND security notes: algorithm HS256 pinned by name on issue and verify (never negotiated), secret CSPRNG >= 32 bytes held exclusively by auth-service and gateway with startup length assertion, password storage bcrypt-hashed never reversible; operations EXACTLY: POST /auth/signup public (request EmailPassword {email format email, password minLength 8}; 201 returns User {id string, email, roles array of string, createdAt date-time}; 409 $ref Conflict with code DUPLICATE_EMAIL), POST /auth/login public (same request; 200 returns AccessTokenPair {accessToken string, user User} — user shape IDENTICAL to /me response per PITFALLS consistency rule; 401 $ref Unauthorized), GET /auth/me secured via security: [{bearerAuth: []}] (200 returns the same User schema; 401 otherwise); NO logout path exists; instead an info.description sentence records D-03 verbatim in spirit: logout is client-side token discard by design in v1 — no endpoint will be added without a contract change. Every operationId camelCase (signup, login, getMe). Every 4xx/5xx references shared responses. All IDs type string; createdAt uses type string format date-time (ISO 8601 ms UTC per interop rules). Then run bash scripts/check-contracts.sh — the tracer proof.</action>
  <verify><automated>bash scripts/check-contracts.sh</automated></verify>
  <done>Gate exits 0 with the auth spec passing Spectral at --fail-severity=warn; the full chain (lint → eol law → coverage greps) proven end-to-end on one real contract before any expansion spec is written.</done>
  <acceptance_criteria>`bash scripts/check-contracts.sh` exit 0; `grep -c "operationId" docs/api-contracts/auth-service.openapi.yaml` >= 3; `grep -c "bearerAuth" docs/api-contracts/auth-service.openapi.yaml` >= 1; `grep -ci "logout" docs/api-contracts/auth-service.openapi.yaml` >= 1 (the D-03 note); `grep -cE "type: number" docs/api-contracts/_shared.yaml` == 0.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine | npx executes third-party code during lint |
| contract file → Phase 7 gateway implementation | Public-vs-protected classification encoded here becomes enforced authorization behavior |
| unauthenticated client → auth endpoints | Signup/login accept untrusted credentials input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npx @stoplight/spectral-cli install/run | high | mitigate | Blocking human-verify checkpoint (Task 1) with npmjs/GitHub evidence review; Docker-image and defer-to-Phase-2 escape hatches pre-approved |
| T-02-01 | Spoofing | JWT verification contract | critical | mitigate | HS256 pinned by name on issue AND verify sides (never negotiated); iss/aud asserted values frozen; forged-token rejection tests assigned to Phase 7 per research |
| T-02-02 | Information Disclosure | Error envelope | medium | mitigate | Shared Error schema forbids internals in message (no hostnames/stack traces/driver errors); correlation via stable code field |
| T-02-03 | Elevation of Privilege | /auth/me classification | high | mitigate | Per-operation security declaration: /me requires bearerAuth explicitly; signup/login declared public — matrix accuracy is acceptance-grepped |
| T-02-04 | Spoofing | Secret handling conventions | high | mitigate | CSPRNG >= 32 bytes requirement, holder restriction (auth+gateway only), startup length assertion written into spec description |
</threat_model>

<verification>
- Full gate green: `bash scripts/check-contracts.sh` exit 0
- Spectral lint clean at --fail-severity=warn on both yaml files
- Topic validator proven to fail on malformed fixture (negative test in Task 2 acceptance)
</verification>

<success_criteria>
- [ ] Gate script covers all five CONTR dimensions mechanically
- [ ] Legitimacy checkpoint passed before any package execution
- [ ] _shared.yaml + auth spec lint clean; tracer chain proven
- [ ] D-03 no-logout note present in auth spec
</success_criteria>

<output>
Create `.planning/phases/01-contracts-repo-scaffolding/01-02-SUMMARY.md` when done
</output>
