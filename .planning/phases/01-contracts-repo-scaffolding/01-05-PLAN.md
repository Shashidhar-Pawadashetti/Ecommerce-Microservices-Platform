---
phase: 01-contracts-repo-scaffolding
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - docs/api-contracts/orders-service.openapi.yaml
  - docs/kafka-topics.md
autonomous: true
requirements: [CONTR-01, CONTR-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "kafka-topics.md specifies both topics so completely that either side of either topic can be implemented in any of the three languages without asking questions (key, partitions, RF, cleanup, producer, consumer groups, payload schema, delivery semantics, per-consumer dedup, DLQ stance)"
    - "payment.completed carries the frozen outcome enum APPROVED|DECLINED with reason present iff DECLINED"
    - "Email rendering inputs are frozen as a table (subject template, from address, body fields) for both APPROVED and DECLINED variants — Phase 6 renders exactly this and the Phase 9 smoke test asserts it via Mailpit REST"
    - "Orders contract freezes idempotent-replay semantics: first POST /orders returns 201, replayed Idempotency-Key returns 200 with the ORIGINAL order body"
    - "The canonical status enum is PENDING_PAYMENT → PAID | PAYMENT_FAILED with terminal-state guards documented as idempotent under redelivery"
  artifacts:
    - "docs/api-contracts/orders-service.openapi.yaml (4 operations incl. /health; OrderStatus enum; snapshot item schema)"
    - "docs/kafka-topics.md (both topic contracts + email content payload tables)"
  key_links:
    - "order.created payload field names/types mirror orders-service.openapi.yaml schemas exactly (single drift surface, checked by validator + review)"
    - "validate-topic-schemas.mjs REQUIRED maps assert kafka-topics.md fenced JSON blocks parse and carry mandatory keys"
    - "OrderStatus enum in orders spec and status wording in email tables both use D-01 spellings"
  prohibitions:
    - statement: "No bare PENDING status value — the initial order status is spelled PENDING_PAYMENT everywhere (D-01); ARCHITECTURE.md's older spelling is superseded"
      status: forbidden
      verification: "grep -nE 'PENDING([^_]|$)' docs/api-contracts/orders-service.openapi.yaml docs/kafka-topics.md outputs nothing"
    - statement: "No MailHog references — the assertion surface is Mailpit REST /api/v1/messages (D-07 approved deviation)"
      status: forbidden
      verification: "grep -icE 'mailhog' docs/kafka-topics.md == 0"
    - statement: "No float money or non-integer cents anywhere in payloads/examples"
      status: forbidden
      validation: "validator asserts Number.isInteger on totalCents/unitPriceCents"
      verification: "grep -cE 'type: number' docs/api-contracts/orders-service.openapi.yaml == 0; validate-topic-schemas.mjs integer assertions pass"
---

<objective>
Author the final REST contract (orders) and the event backbone (kafka-topics.md) — completing CONTR-01 coverage and delivering CONTR-02 wholesale.

Purpose: These are the highest-drift-risk documents in the repo: the saga pair (Phase 5), the notification worker (Phase 6), and the polling frontend (Phase 8) all implement from them. Every ambiguity resolved here is a future integration failure prevented.

Output: `orders-service.openapi.yaml`, `docs/kafka-topics.md`, gate green, topic-schema validator passing on real content.

Locked decisions implemented here:
- **D-01**: canonical enum spellings PENDING_PAYMENT | PAID | PAYMENT_FAILED (requirement text wins over the architecture sketch's shorter form)
- **D-05**: idempotent replay returns HTTP 200 with original order; first creation returns 201
- **D-06**: email payload = rendering-input contract (subject/from/body fields per variant), asserted later via Mailpit REST
- **D-checkout-source**: POST /orders takes NO items body — contents come from the user's live cart via the internal read; the order snapshots items AND prices at creation time
</objective>

<execution_context>
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Pattern 3 full topic template + frozen enum quote, §Contract Content Inventory orders table, §Open Questions Q1/Q5/Q6 recommendations)
@.planning/phases/01-contracts-repo-scaffolding/01-02-SUMMARY.md (gate + shared components)
@.planning/phases/01-contracts-repo-scaffolding/01-03-SUMMARY.md (interop canon to follow: string IDs, integer cents, ISO-ms dates)
</context>

<precondition>scripts/check-contracts.sh and scripts/validate-topic-schemas.mjs exist (Plan 02); _shared.yaml exposes IdempotencyKey parameter and Error envelope.</precondition>

<tasks>

<task type="auto">
  <name>Task 1: orders-service.openapi.yaml — idempotent checkout, status machine, polling detail</name>
  <files>docs/api-contracts/orders-service.openapi.yaml</files>
  <read_first>.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Contract Content Inventory orders table, §Pattern 2 excerpt showing 201/200 replay pattern, §Open Questions (RESOLVED) Q1+Q5), docs/json-interop.md (money/date rules). NOTE: do NOT read docs/api-contracts/cart-service.openapi.yaml here — it is authored by same-wave Plan 04 under parallel execution; the CartLine shape feeding checkout is already inlined in this task's action (OrderItem mirrors CartLine exactly: productId, nameSnapshot, unitPriceCents, quantity)</read_first>
  <action>OpenAPI 3.0.3 spec titled Orders Service API v1.0.0 with authority header. info.description records: all operations require valid JWT except /health; identity from sub; POST /orders derives contents from the caller's live cart via network-internal GET /cart/{userId} and SNAPSHOTS items AND prices at creation (later catalog changes never alter an order); produces order.created to Kafka after persistence (ORDR-02).
  Operations EXACTLY four:
  POST /orders SECURED — parameters: $ref ./_shared.yaml#/components/parameters/IdempotencyKey (REQUIRED per ORDR-05); requestBody ABSENT (contents sourced from cart — document this explicitly in description); responses: 201 OrderSnapshot {orderId string, userId string, status OrderStatus (= PENDING_PAYMENT on creation), items array OrderItem, totalCents integer, currency string, createdAt date-time} header Location /orders/{orderId}; 200 IDENTICAL OrderSnapshot schema with description idempotent replay — same Idempotency-Key returns the ORIGINAL order, no duplicate created (D-05: 200 not 201 distinguishes replay honestly); 400 ValidationError code VALIDATION_FAILED (empty cart, missing header); 401 Unauthorized.
  GET /orders SECURED — 200 OrderList {items array of OrderSummary, total integer}; description: history filtered server-side by token sub owner; OrderSummary = orderId, status, totalCents, createdAt.
  GET /orders/{id} SECURED — path id string; 200 OrderSnapshot (full detail incl. status — THE frontend polling endpoint for terminal-state detection); 404 NotFound.
  GET /health PUBLIC network-internal — 200 {status string}.
  Schema OrderItem: productId string, nameSnapshot string (catalog name FROZEN at purchase), unitPriceCents integer minimum 0, quantity integer minimum 1. Schema OrderStatus: type string enum EXACTLY [PENDING_PAYMENT, PAID, PAYMENT_FAILED] with description: transitions PENDING_PAYMENT→PAID and PENDING_PAYMENT→PAYMENT_FAILED driven by payment.completed consumption under terminal-state guards — once terminal, further deliveries of either outcome are acknowledged and ignored (idempotent under at-least-once redelivery, ORDR-04). operationIds camelCase (createOrder, listOrders, getOrder, health). Money integer-cents only; dates ISO-ms date-time.</action>
  <verify><automated>npx @stoplight/spectral-cli@6.16.3 lint docs/api-contracts/orders-service.openapi.yaml --ruleset docs/api-contracts/.spectral.yaml --fail-severity=warn exits 0</automated></verify>
  <done>Orders contract complete, lint-clean, replay semantics + status machine frozen per D-01/D-05.</done>
  <acceptance_criteria>`bash scripts/check-contracts.sh` exit 0; `grep -q "IdempotencyKey" docs/api-contracts/orders-service.openapi.yaml` succeeds; `grep -c "PENDING_PAYMENT" docs/api-contracts/orders-service.openapi.yaml` >= 2; `grep -nE 'PENDING([^_]|$)' docs/api-contracts/orders-service.openapi.yaml` outputs nothing; `grep -q "'200'" docs/api-contracts/orders-service.openapi.yaml && grep -q "'201'" docs/api-contracts/orders-service.openapi.yaml` both succeed (replay + create codes present).</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: docs/kafka-topics.md — both topic contracts + email payload tables</name>
  <files>docs/kafka-topics.md</files>
  <read_first>.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Pattern 3 complete template incl. required-field checklist and verbatim payload quotes, §State of the Art Mailpit row), docs/json-interop.md (payloads obey interop law), scripts/validate-topic-schemas.mjs (REQUIRED key maps this doc must satisfy)</read_first>
  <action>Create docs/kafka-topics.md with intro stating: JSON schemas inline (no Schema Registry in v1 — explicit exclusion), payloads obey docs/json-interop.md (string IDs, integer cents, ISO-8601 ms UTC createdAt/processedAt), topics auto-create OFF, declared here before producers exist.
  For EACH of order.created and payment.completed provide the full checklist table: name; Partitions 3 (declared up-front — partition counts immutable post-creation); Replication factor 1 (single-broker dev override); Key strategy order.created keys by orderId string giving per-order ordering within a partition, payment.completed also keyed orderId so per-order events land ordered; Cleanup policy delete with retention >= 7 days (replay/debug window); Producer order-service / payment-service respectively; Consumers listed BY GROUP — payment-service + notification-service consume order.created, order-service + notification-service consume payment.completed; Delivery semantics at-least-once, consumers MUST be idempotent; DLQ stance none in v1 — poison messages log-and-skip (mock domain tolerates).
  Payload sections: fenced json blocks matching validator REQUIRED sets EXACTLY. order.created: eventId uuid string, orderId string, userId string, userEmail string, items array of {productId, nameSnapshot, unitPriceCents integer, quantity integer}, totalCents integer, currency ISO-4217 string, createdAt ISO-ms UTC string. payment.completed: eventId uuid string, orderId string, outcome string enum APPROVED|DECLINED (frozen spelling), reason string PRESENT ONLY when outcome DECLINED (absent otherwise — absent-not-null rule), processedAt ISO-ms UTC string.
  Dedup contract subsection per consumer: payment-service skips order.created if orderId already authorized; notification-service dedupes BOTH topics on eventId; order-service ignores payment.completed for already-terminal orders (guards).
  Email Content Payload section (rendering-input contract per D-06): From address orders@ecommerce.local; subject templates — APPROVED: "Order {orderId} confirmed"; DECLINED: "Order {orderId} payment failed". Body field tables per variant: APPROVED renders order identifier (orderId), item summary lines (nameSnapshot × quantity each), total line (totalCents minor units formatted with currency symbol), status word PAID; DECLINED renders orderId, the DECLINED reason string verbatim, status PAYMENT_FAILED, plus one next-step sentence directing retry via new checkout. Assertion note: Phase 6 renders exactly these fields; Phase 9 smoke test asserts delivery programmatically through Mailpit's REST API endpoint /api/v1/messages (Mailpit is the mock SMTP per approved deviation). Cross-consistency sentence: payload field names mirror orders-service.openapi.yaml schemas deliberately — change both together or the gate fails review.</action>
  <verify><automated>node scripts/validate-topic-schemas.mjs docs/kafka-topics.md && bash scripts/check-contracts.sh</automated></verify>
  <done>Both topics fully contracted; validator passes on real content; email rendering inputs frozen for Phases 6 and 9.</done>
  <acceptance_criteria>`node scripts/validate-topic-schemas.mjs docs/kafka-topics.md` exit 0; `bash scripts/check-contracts.sh` exit 0; `grep -c "^## Topic:" docs/kafka-topics.md` == 2; `grep -q "APPROVED" docs/kafka-topics.md && grep -q "DECLINED" docs/kafka-topics.md` succeed; `grep -ciE "mailhog" docs/kafka-topics.md` == 0; `grep -q "/api/v1/messages" docs/kafka-topics.md` succeeds; `grep -nE 'PENDING([^_]|$)' docs/kafka-topics.md` outputs nothing.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kafka consumers ↔ producers | At-least-once redelivery means duplicate/malicious event replays cross service boundaries |
| authenticated client → orders endpoints | Checkout must not trust client claims about cart contents |
| email renderer → recipient mailbox | Event fields flow verbatim into emails |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | Orders endpoints classification | high | mitigate | All three REST ops declare bearerAuth; /health alone public-internal — matrix transcribed by Phase 7 verbatim |
| T-05-02 | Tampering | Redelivery/duplicate events | high | mitigate | Per-consumer dedup contracts mandated (orderId authorization check, eventId dedupe, terminal-state guards) — written INTO the topic contract |
| T-05-03 | Repudiation | Replay ambiguity | medium | mitigate | D-05 locked: 201 create vs 200 replay distinguishable at HTTP level; Idempotency-Key required header |
| T-05-04 | Information Disclosure | userEmail into events/emails | low | accept | User's own email to their own address is the feature (NOTF-02); no third-party recipients exist in v1 |
| T-05-SC | Tampering | npm installs | high | mitigate | No package installs in this plan; Spectral legitimacy handled at Plan 02 Task 1 (T-02-SC) |
</threat_model>

<verification>
- Gate green across all five specs + topics file
- Topic-schema validator passes on real payloads and its REQUIRED maps match the authored fields exactly
- Enum spellings provably canonical (negative grep on bare pending token)
</verification>

<success_criteria>
- [ ] orders spec lint-clean; replay 200/create 201 encoded; enum D-01
- [ ] Both topic contracts carry every Pattern 3 checklist field
- [ ] Email payload tables complete for APPROVED + DECLINED; Mailpit REST named
</success_criteria>

<output>
Create `.planning/phases/01-contracts-repo-scaffolding/01-05-SUMMARY.md` when done
</output>
