---
phase: 01-contracts-repo-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - .editorconfig
  - .gitignore
  - README.md
  - docker-compose.yml
  - .env.example
  - services/api-gateway/.gitkeep
  - services/auth-service/.gitkeep
  - services/catalog-service/.gitkeep
  - services/cart-service/.gitkeep
  - services/order-service/.gitkeep
  - services/payment-service/.gitkeep
  - services/notification-service/.gitkeep
autonomous: false
requirements: [CONTR-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - ".gitattributes is committed before ANY service file exists, so no CRLF-poisoned blob can ever enter history"
    - "A fresh clone on Windows yields LF working-tree endings for every governed file regardless of the cloner's core.autocrlf setting"
    - "All seven monorepo service directories exist under services/ matching build-plan §2 layout"
    - ".env.example declares every environment variable name Phases 2–9 will need, with placeholder values only"
    - "Root README orients a fresh-context agent to docs/, contracts-first policy, and the attribute no-override rule"
  artifacts:
    - ".gitattributes (text=auto eol=lf catch-all + *.bat/*.cmd crlf + binary exceptions)"
    - ".editorconfig (UTF-8, lf, per-language indent)"
    - ".gitignore (extended: .env, node_modules/, target/, __pycache__/, .venv/, dist/, *.log, .planning/research/.cache/)"
    - "README.md"
    - "docker-compose.yml (placeholder header comment)"
    - ".env.example"
    - "services/{api-gateway,auth-service,catalog-service,cart-service,order-service,payment-service,notification-service}/.gitkeep"
  key_links:
    - ".gitattributes governs every future commit — its catch-all rule pairs text WITH eol=lf (bare text is insufficient on Windows)"
    - ".gitignore excludes .env from tracking from commit one (secrets hygiene pairing with .env.example)"
  prohibitions:
    - statement: "No real secret values in .env.example — placeholder values only"
      status: forbidden
      verification: "grep -nE '(SECRET|PASSWORD)=' .env.example shows only change-me-style placeholders; no value passes a hex/base64 entropy spot-check beyond defaults like 3600"
    - statement: "No CRLF in the git index under governed paths (anything except *.bat/*.cmd)"
      status: forbidden
      verification: "git ls-files --eol | grep 'i/crlf' | grep -vE '\\.(bat|cmd)$' produces empty output"
    - statement: "No host bind-mount or real infrastructure in docker-compose.yml — Phase 1 ships a placeholder header only"
      status: forbidden
      verification: "docker-compose.yml contains comments only, no service: keys"
---

<objective>
Establish repo hygiene law and the monorepo skeleton BEFORE any contract or code file is authored, and clear the two human governance gates that STATE.md and the package-legitimacy protocol place ahead of the contracts freeze.

Purpose: CONTR-04's success criterion ("a fresh clone on Windows yields LF everywhere governed") is only achievable if `.gitattributes` precedes every other content commit. The sign-offs unblock Plans 03–05, whose manifests and docs reference Mailpit, PyMongo AsyncMongoClient, and the Boot 3.5 pin by name.

Output: `.gitattributes`/`.editorconfig`/extended `.gitignore` (renormalized), seven service dirs, root README, compose placeholder, complete `.env.example`.

Locked contract decisions recorded here (referenced by later plans):
- **D-07 (gate)**: Stakeholder approves research deviations before freeze — MailHog→**Mailpit**, Motor→**PyMongo AsyncMongoClient**, Spring Boot pinned at **3.5.16** (OSS EOL accepted for local learning target, noted in runbook later)
</objective>

<execution_context>
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Pattern 4 .gitattributes semantics, §Recommended Project Structure, §Security Domain V14)
@.planning/phases/01-contracts-repo-scaffolding/01-VALIDATION.md
@.planning/STATE.md (Blockers: pre-phase sign-offs)
@ecommerce-microservices-build-plan.md (§2 Repo Strategy — monorepo layout)
</context>

<precondition>Git repo already initialized with only planning-artifact commits (no product/service files tracked — verify with `git ls-files` showing nothing under services/ or frontend/); no .gitattributes tracked yet. Do not gate on a specific HEAD hash or commit count — planning commits continue to land.</precondition>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Pre-freeze stakeholder sign-off on research deviations (per D-07)</name>
  <files>.planning/phases/01-contracts-repo-scaffolding/01-01-SUMMARY.md</files>
  <read_first>.planning/STATE.md (Blockers/Concerns section), .planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (Open Question 7, State of the Art table)</read_first>
  <decision>Approve the three research deviations before the contracts freeze (STATE.md lists these as pre-phase blockers)</decision>
  <context>
    Contracts authored this phase reference the mock SMTP by name, the Mongo driver in the manifest, and the Spring Boot pin — all three later phases copy these values mechanically. Changing any of them after Plans 03–05 ship means rewriting frozen contracts. The researched recommendations are: (a) mock SMTP is Mailpit (axllent/mailpit), replacing the source plan's MailHog — drop-in on ports 1025/8025, actively maintained, REST API at /api/v1/messages becomes the Phase 9 smoke-test assertion surface; (b) Mongo async driver is PyMongo AsyncMongoClient (pymongo >=4.9), replacing deprecated Motor; (c) Spring Boot stays pinned at 3.5.16 with Spring Cloud 2025.0.3 despite OSS support ending 2026-06-30 — acceptable for a local learning target.
  </context>
  <options>
    <option id="approve-all">
      <name>Approve all three as researched</name>
      <pros>Matches every verified pin in STACK.md; zero replanning downstream; unblocks Waves 2–3 immediately</pros>
      <cons>Boot 3.5 is past OSS EOL (noted for the Phase 10 runbook)</cons>
    </option>
    <option id="bump-boot">
      <name>Approve Mailpit + PyMongo, but bump the Java trio to Boot 4.0.x + Spring Cloud 2025.1 (Oakwood)</name>
      <pros>Actively OSS-supported Java line through ~2027</pros>
      <cons>Plans 03–05 manifest rows and gateway-starter notes change before freeze; re-check tutorial drift</cons>
    </option>
    <option id="reject">
      <name>Reject one or more deviations</name>
      <pros>Honors original source-plan naming</pros>
      <cons>Halts the phase: contracts/docs referencing the rejected names must be replanned; reintroduces unmaintained-dependency risk</cons>
    </option>
  </options>
  <resume-signal>Reply: approve-all | bump-boot | reject (with notes). Record outcomes in the plan SUMMARY.</resume-signal>
  <verify>Decision outcomes recorded in .planning/phases/01-contracts-repo-scaffolding/01-01-SUMMARY.md under a "Decisions" heading naming Mailpit, PyMongo AsyncMongoClient, and the Boot pin explicitly.</verify>
  <done>All three deviations either approved (execution continues) or rejected (halt + escalate to orchestrator).</done>
  <acceptance_criteria>SUMMARY.md contains the three decision outcomes; continuation implies approval.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Line-ending law — .gitattributes first among content commits + renormalize</name>
  <files>.gitattributes, .editorconfig, .gitignore</files>
  <read_first>.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (Pattern 4 — verified semantics: bare `text` does NOT force LF checkout on Windows; `text eol=lf` does), .gitignore (current content is a single `.opencode` line)</read_first>
  <action>Create .gitattributes with EXACTLY this rule set per research Pattern 4: catch-all `* text=auto eol=lf`; Windows batch exceptions `*.bat text eol=crlf` and `*.cmd text eol=crlf` (cmd.exe label/goto fragility under LF — conservative choice per assumption A1); binary exceptions `*.png binary`, `*.jpg binary`, `*.jpeg binary`, `*.gif binary`, `*.ico binary` (seed product images arrive Phase 3); self-documenting explicit line `mvnw text eol=lf` (redundant with catch-all but intentional per research note). Create .editorconfig: root=true, charset=utf-8, end_of_line=lf, insert_final_newline=true, trim_trailing_whitespace=true; `[*.md]` keeps trailing whitespace (line-break convention); indent 2 spaces for yaml/yml/json/js/ts, 4 for py, tab-indented block for Makefile if ever added. Extend .gitignore (preserve existing `.opencode` line) with: `.env`, `.env.*` except `!.env.example`, `node_modules/`, `target/`, `__pycache__/`, `.venv/`, `dist/`, `build/`, `*.log`, `.DS_Store`, `.planning/research/.cache/`. THEN, because planning docs were committed before attributes existed (repo privilege partially spent — planning-only commits precede this work), run `git add --renormalize .` ONCE and commit .gitattributes + .editorconfig + .gitignore as their own commit BEFORE any scaffold file lands.</action>
  <verify><automated>git ls-files --eol | grep "i/crlf" | grep -vE "\.(bat|cmd)$" produces empty output; git log --oneline -1 shows the attributes commit preceding scaffold commits</automated></verify>
  <done>Attributes are law: index holds zero CRLF blobs on governed paths; future checkouts produce LF worktrees on Windows.</done>
  <acceptance_criteria>`git ls-files --eol | grep "i/crlf" | grep -vE "\\.(bat|cmd)$"` outputs nothing (exit-checked); `git check-attr eol -- .env.example` reports `eol: lf`; .gitignore contains `.env` and `!.env.example` lines.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Monorepo skeleton + README + .env.example (placeholder-values-only)</name>
  <files>README.md, docker-compose.yml, .env.example, services/api-gateway/.gitkeep, services/auth-service/.gitkeep, services/catalog-service/.gitkeep, services/cart-service/.gitkeep, services/order-service/.gitkeep, services/payment-service/.gitkeep, services/notification-service/.gitkeep</files>
  <read_first>ecommerce-microservices-build-plan.md §2 (tree layout), .planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Recommended Project Structure, §Security Domain V14 row, Pitfall 11 linkage)</read_first>
  <action>Create the seven service directories exactly as build-plan §2 names them (api-gateway, auth-service, catalog-service, cart-service, order-service, payment-service, notification-service), each holding only a .gitkeep (git tracks no empty dirs). Write README.md: one-paragraph project overview; architecture pointer to docs/ (noting contracts land this phase); the contracts-first policy statement (committed specs are the source of truth, changes start there); contributor note "do not override .gitattributes via .git/info/attributes"; quick-start stub saying docker-compose grows per phase. Write docker-compose.yml containing ONLY a header comment block (project title, "grows incrementally starting Phase 2 per build plan §4") — no service keys. Write .env.example covering every variable name later phases consume, grouped with comments, ALL placeholder values: KAFKA_BOOTSTRAP_SERVERS=localhost:9092; JWT_SECRET=<long-random-secret-change-me>; JWT_ISSUER=ecommerce-auth; JWT_AUDIENCE=ecommerce-api; JWT_TTL_SECONDS=3600; PAYMENT_MODE=always_success (one of always_success|always_fail|random per ORDR-03); CART_TTL_SECONDS=1209600; CATALOG_SERVICE_URL=http://catalog-service:8000; CART_SERVICE_URL=http://cart-service:3001; SMTP_HOST=mailpit; SMTP_PORT=1025; POSTGRES_USER=ecommerce; POSTGRES_PASSWORD=change-me-db-password; MONGO_URI=mongodb://mongo:27017; REDIS_URL=redis://redis:6379/0. Policy comment at top: names are contractual — later phases ADD variables, never rename; values here are placeholders, copy to .env locally, never commit .env. Note next to SMTP vars that Mailpit consumes them (per D-07 approval).</action>
  <verify><automated>test -d each of the seven services/<name> directories succeeds; grep -c "=" .env.example >= 14; every SECRET/PASSWORD line value matches change-me placeholder style</automated></verify>
  <done>Skeleton matches build-plan §2; env var names frozen for Phases 2–9; zero real credentials anywhere.</done>
  <acceptance_criteria>`ls services` lists exactly the seven service directories; `grep -nE "(SECRET|PASSWORD)=" .env.example` shows only change-me placeholders; `grep -c "^#" docker-compose.yml` > 0 and `grep -c "^[a-zA-Z_-]*:" docker-compose.yml` == 0 (comments only); README.md contains the phrase "contracts-first".</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer workstation → git history | Everything committed here becomes immutable context for fresh-context milestone agents |
| .env.example → .env copies | Variable NAMES are contractual; values must never be real |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.example | high | mitigate | Placeholder-only values enforced by acceptance grep; .gitignore excludes .env (but not !.env.example) from commit one |
| T-01-02 | Tampering | .gitattributes / shell scripts / mvnw | high | mitigate | `text eol=lf` pairing (bare text insufficient on Windows); renormalize pass cleans pre-attribute blobs; mechanical i/crlf gate in Plan 02's script reuses this check |
| T-01-03 | Denial of Service | docker-compose.yml | low | accept | Placeholder-comment-only file cannot misconfigure anything; real compose arrives Phase 2+ |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs occur in this plan; Spectral legitimacy gate lives in Plan 02 Task 1 (T-02-SC) |
</threat_model>

<verification>
- `git ls-files --eol` clean of i/crlf on governed paths
- Seven service dirs present; README/compose/env-example match acceptance criteria
- SUMMARY records D-07 sign-off outcomes
</verification>

<success_criteria>
- [ ] Attributes commit precedes all scaffold commits; index provably CRLF-free
- [ ] Skeleton mirrors build-plan §2 exactly
- [ ] .env.example names frozen, values placeholder-only
- [ ] D-07 decisions approved and recorded (or halt escalated)
</success_criteria>

<output>
Create `.planning/phases/01-contracts-repo-scaffolding/01-01-SUMMARY.md` when done
</output>
