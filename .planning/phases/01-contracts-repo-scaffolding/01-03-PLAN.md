---
phase: 01-contracts-repo-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/versions.md
  - docs/json-interop.md
autonomous: true
requirements: [CONTR-05, CONTR-03]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every stack component's exact pin lives in ONE authoritative file (docs/versions.md) that fresh-context milestone agents read before writing any build file — no per-service VERSION files ever exist"
    - "The five cross-language JSON interop rules are documented with their per-runtime failure modes, giving Phases 2–9 a citable normative reference"
    - "JWT claims and secret-handling conventions have a single canonical home that the auth spec and kafka-topics.md link to rather than duplicate-authority"
    - "Manifest completeness is mechanically checkable by Plan 02's gate stage 4 (one grep per required component)"
  artifacts:
    - "docs/versions.md (~20-row pin table: Component | Exact pin | Kind | Consumed by | Note; verified-date footer; renamed-starter note for Phase 7)"
    - "docs/json-interop.md (five rules + per-runtime failure modes + JWT claims table + secret-handling section)"
  key_links:
    - "docs/versions.md rows are copied MECHANICALLY from STACK.md pins (AGENTS.md embedded table) — zero re-research"
    - "docs/api-contracts/_shared.yaml bearerFormat comment (Plan 02) points here as canonical claims reference"
    - "check-contracts.sh stage 4 greps this manifest's pins (Plan 02) — completeness enforced at every future phase verify"
  prohibitions:
    - statement: "No MailHog, Motor, or legacy spring-cloud-starter-gateway artifact references anywhere in the manifest or interop doc"
      status: forbidden
      verification: "grep -inE '(mailhog|\\bmotor\\b|spring-cloud-starter-gateway[^-])' docs/versions.md docs/json-interop.md outputs nothing"
    - statement: "No approximate pins ('latest', '3.x', '>=') for framework/runtime/image components in versions.md except where the source STACK.md itself records an open floor (pymongo >=4.9)"
      status: forbidden
      verification: "grep -cE '\\| *(latest|[0-9]+\\.x) *\\|' docs/versions.md == 0"
---

<objective>
Freeze the version truth and the JSON interop law — the two documents every later milestone reads FIRST — as Wave-2 parallel work alongside Plan 02's validation gate.

Purpose: CONTR-05 exists so fresh-context agents never improvise a version; CONTR-03 exists so three languages with no shared types serialize identically. Both are prose artifacts whose enforcement hook (gate stage 4 / schema encoding in specs) already lands via Plans 02/04/05.

Output: `docs/versions.md`, `docs/json-interop.md`.

Decisions implemented here:
- **D-07 outcomes** (from Plan 01 Task 1): Mailpit named (not MailHog), PyMongo AsyncMongoClient named (not Motor), Boot 3.5.16 pinned with EOL-posture note
- **D-JWT canonical**: the claims table (sub/email/roles/iss=ecommerce-auth/aud=ecommerce-api/iat/exp epoch seconds, TTL ≈3600 s ±60 s skew, HS256) and secret rules live HERE
</objective>

<execution_context>
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md (embedded STACK.md tables lines 17–54 — THE source for every pin; copy mechanically)
@.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Pattern 5 manifest format + completeness checklist, §Contract Content Inventory cross-cutting decisions, §Security Domain V3/V6, §State of the Art deprecated-names list)
</context>

<precondition>Plan 01 Task 1 recorded D-07 approvals in .planning/phases/01-contracts-repo-scaffolding/01-01-SUMMARY.md (Mailpit / PyMongo AsyncMongoClient / Boot 3.5.16 posture).</precondition>

<tasks>

<task type="auto">
  <name>Task 1: docs/versions.md — pinned version manifest copied from STACK.md</name>
  <files>docs/versions.md</files>
  <read_first>AGENTS.md (STACK.md section — authoritative pins table), .planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Pattern 5 format + component checklist, §Common Pitfalls #5)</read_first>
  <action>Create docs/versions.md as a single markdown table with columns Component | Exact pin | Kind | Consumed by | Note. Rows (ALL from STACK.md, no independent version research): Spring Boot 3.5.16 (framework, api-gateway/auth/order); Spring Cloud BOM 2025.0.3 Northfields (BOM, api-gateway; note ONLY the renamed artifact spring-cloud-starter-gateway-server-webflux is valid — Phase 7 must not use the deprecated predecessor name); JDK 21 LTS (runtime, all Java services); FastAPI 0.141.1 installed as fastapi[standard] (framework, catalog/payment); Pydantic 2.13.4 + pydantic-settings (schema/config, catalog/payment); Python 3.13 on base image python:3.13-slim (runtime, catalog/payment); Node.js 24 LTS line (runtime, cart/notification/frontend tooling); Express 5.2.1 (framework, cart); ioredis 6.0.0 (library, cart); kafkajs 2.2.4 (library, notification; note maintenance status acknowledged per research — migration path documented if ever needed); nodemailer 9.0.5 (library, notification); aiokafka 0.14.0 (library, payment); pymongo >=4.9 using AsyncMongoClient (library, catalog; explicitly NOT Motor — deprecated); apache/kafka 4.2.1 image KRaft combined broker+controller (infrastructure image, event bus); postgres 18 image (datastore image, users+orders; volume mounted at /var/lib/postgresql per PG18 layout change — note it); mongo 8.0 image long-support line (datastore image, products); redis 8-alpine line (datastore image, carts); Next.js 16.3.2 with React 19 (framework, frontend); Mailpit axllent/mailpit latest-tagged (mock SMTP :1025 + UI :8025 + REST /api/v1Messages assertion surface — spelled /api/v1/messages); eclipse-temurin 21-jre base images alpine/noble variants (container base, Java runtimes). Footer line: Verified 2026-08-24 against registry dist-tags/npm view/PyPI pages (per STACK.md sources). Header sentence: this file is the single source of truth — pom.xml/package.json/pyproject.toml/compose image tags COPY from here; do not create per-service version files.</action>
  <verify>grep -q "Spring Boot.*3\.5\.16" docs/versions.md succeeds; equivalent grep succeeds for each of the ~20 required components; row count >= 20.</verify>
  <done>Manifest complete per Pattern 5 checklist; fresh-context agents have one authoritative read.</done>
  <acceptance_criteria>`grep -c "^| " docs/versions.md` >= 21 (header + ~20 rows); `grep -qE "Verified 2026-08-24" docs/versions.md` succeeds; `grep -qi "mailpit" docs/versions.md` succeeds; `grep -qi "mailhog" docs/versions.md` fails (no match); `grep -qiE "\bmotor\b" docs/versions.md` matches only inside the explicit not-Motor caveat sentence if present, never as a chosen dependency — simplest proof: the AsyncMongoClient row exists (`grep -q "AsyncMongoClient" docs/versions.md`).</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: docs/json-interop.md — five interop rules + JWT claims/secret canon</name>
  <files>docs/json-interop.md</files>
  <read_first>.planning/phases/01-contracts-repo-scaffolding/01-RESEARCH.md (§Pitfall 4 encode-don't-prose, §Standard Stack JSON Schema row, §Contract Content Inventory cross-cutting decisions incl. JWT claim values verbatim, §Security Domain V3/V5/V6 rows, PITFALLS research five-rule set referenced therein)</read_first>
  <action>Create docs/json-interop.md with five numbered rule sections, each stating: the rule, the wire-level constraint, and per-runtime failure modes. Rule 1 Dates/times: ISO 8601 strings, format date-time, millisecond precision UTC (e.g. 2026-08-24T12:00:00.123Z); failure modes — Pydantic microseconds truncation, Jackson default serialization drift without config. Rule 2 Money: integer minor units only; field names end in Cents (priceCents, totalCents, unitPriceCents, lineTotalCents); type integer; floats forbidden on the wire AND in examples; currency travels beside totals as ISO 4217 string; failure mode — binary float rounding corruption across all three runtimes. Rule 3 Identifiers: string IDs everywhere on the wire even when storage uses bigint/serial/ObjectId hex; format int64 forbidden on any wire-facing ID; failure mode — JS consumers lose precision beyond Number.MAX_SAFE_INTEGER. Rule 4 Nullability: optional = absent key; explicit null forbidden unless a schema declares nullable true (OpenAPI 3.0.3 discipline); readers must not require presence of optional keys. Rule 5 Unknown fields: receivers ignore unrecognized properties (reader-side tolerance policy — never writer-side sloppiness); failure modes — Jackson FAIL_ON_UNKNOWN_PROPERTIES must be disabled/configured tolerant, Pydantic extra=ignore default stance documented. Then a JWT Claims section (CANONICAL table): sub (userId string), email (string), roles (array of string), iss exactly ecommerce-auth, aud exactly ecommerce-api, iat/exp epoch seconds, token TTL approximately 3600 s with plus/minus 60 s clock skew accepted; algorithm HS256 pinned by name on issue and verify sides — negotiation forbidden; cookie transport attributes noted for Phases 2/7/8: httpOnly, SameSite=Lax, Path=/ (value recorded, enforcement owned by later phases). Secret Handling section: JWT_SECRET from CSPRNG minimum 32 bytes; held exclusively by auth-service and api-gateway; both assert minimum length at startup and refuse to boot short secrets; supplied via environment (.env locally, compose env in later phases), never logged, never committed (.env.example carries placeholder only). Closing note: these rules bind all three runtimes simultaneously; contract schemas encode them (string IDs, integer cents, date-time formats) — where prose and schema disagree, the schema wins and this doc must be fixed.</action>
  <verify>grep -c "^## Rule" docs/json-interop.md == 5; grep -q "ecommerce-auth" and "ecommerce-api" succeed; grep -qi "HS256" succeeds.</verify>
  <done>The five rules and JWT canon exist as the citable normative reference for all later phases.</done>
  <acceptance_criteria>`grep -c "^## Rule" docs/json-interop.md` == 5; `grep -q "ecommerce-auth" docs/json-interop.md && grep -q "ecommerce-api" docs/json-interop.md` succeeds; `grep -q "Cents" docs/json-interop.md` succeeds (money naming rule); `grep -qiE "FAIL_ON_UNKNOWN_PROPERTIES" docs/json-interop.md` succeeds (Jackson failure mode documented).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning doc → repo manifest | Copy errors here propagate to every future build file |
| interop doc → three language runtimes | A wrong rule breaks serialization in Java, Python, and Node simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | docs/versions.md pins | medium | mitigate | Mechanical copy from STACK.md (verified this session); gate stage-4 greps enforce presence at every future verify |
| T-03-02 | Information Disclosure | JWT secret conventions | high | mitigate | Canon states CSPRNG ≥32 bytes, two-holder restriction, startup assertion, never-log/never-commit rules |
| T-03-03 | Spoofing | Algorithm selection | critical | mitigate | HS256 pinned by name, negotiation explicitly forbidden — canonical wording lives in this doc |
| T-03-04 | Repudiation | Ambiguous rule precedence | low | accept | Schema-wins-over-prose clause removes interpretive wiggle; residual risk negligible for v1 |
</threat_model>

<verification>
- Manifest greps all pass (mirrors gate stage 4 once Plan 02 merges)
- Five-rule structure present; JWT canon values exact
</verification>

<success_criteria>
- [ ] versions.md covers every Pattern 5 checklist component with exact pins + verified footer
- [ ] json-interop.md states five rules with runtime failure modes
- [ ] JWT claims/secret canon singular-homed here; deprecated names absent
</success_criteria>

<output>
Create `.planning/phases/01-contracts-repo-scaffolding/01-03-SUMMARY.md` when done
</output>
