---
phase: 02-auth-service
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - scripts/smoke-auth.sh
  - services/auth-service/README.md
autonomous: true
requirements: [AUTH-04, SC-5]
user_setup: []

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/smoke-auth.sh runs standalone against a live stack and prints an ALL-PASS verdict covering signup, duplicate rejection, login failure, login success, authenticated profile retrieval, anonymous rejection, and the documented-absence behavior of the third write operation (AUTH-04)"
    - "services/auth-service/README.md states the client-side token discard policy with no server revocation in v1, satisfying AUTH-04's documentation criterion"
    - "The packaged Docker image reflects final Wave-2 code (rebuild proven), both compose services are healthy, and the full Maven suite plus contracts gate pass together"
    - "Every file added this phase carries LF line endings per .gitattributes law"
  artifacts:
    - scripts/smoke-auth.sh (jq-free, pure-curl status assertions, house bash conventions)
    - services/auth-service/README.md (endpoints table incl. explicit no-server-logout statement, env vars, memory posture, residual-risk note)
  key_links:
    - "smoke script BASE URL defaults to http://localhost:8081 — the transitional port from Plan 01"
    - "Phase 9 chains this script into the zero-manual-steps E2E (D-10 sibling-script pattern)"
  prohibitions:
    - statement: "No logout endpoint exists in auth-service (frozen contract; client-side token discard only)."
      status: enforced-at-phase-close
      verification: "Smoke script asserts the route answers 404/405 at runtime; controller source contains no such mapping."
    - statement: "Passwords never persist plaintext or reversibly encoded."
      status: enforced-at-phase-close
      verification: "Integration test from Plan 02 asserts bcrypt-prefix storage."
    - statement: "ddl-auto stays validate; Flyway owns schema; shipped migrations append-only."
      status: enforced-at-phase-close
      verification: "application.yml source assertion; migration dir holds only V1 at phase close."
---

<objective>
Close the phase: commit the reusable standalone smoke script (D-10) and service documentation proving the client-side logout policy (AUTH-04), then run the full phase verification gate — rebuilt images, healthy compose stack, green test suite, contracts conformance, LF hygiene — so every roadmap success criterion for Phase 2 is demonstrably true before verify-work.
</objective>

<execution_context>
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-auth-service/02-CONTEXT.md
@.planning/phases/02-auth-service/02-RESEARCH.md
@.planning/phases/02-auth-service/02-PATTERNS.md
@docs/api-contracts/_shared.yaml
@scripts/check-contracts.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: smoke-auth.sh + README logout-policy documentation (AUTH-04)</name>
  <files>scripts/smoke-auth.sh, services/auth-service/README.md</files>
  <read_first>
    - scripts/check-contracts.sh (exact house bash conventions: shebang, set flags, BASH_SOURCE ROOT resolution, boxed header, staged echoes, fail accumulation — copy the skeleton)
    - .planning/phases/02-auth-service/02-PATTERNS.md (adaptation notes for this script, lines ~47-81)
    - .planning/phases/02-auth-service/02-RESEARCH.md (jq-free assertion style in Code Examples; A6 sed-extraction caveat)
    - docs/api-contracts/auth-service.openapi.yaml (info.description logout paragraph lines 36-38 — README paraphrases this policy)
    - docs/api-contracts/_shared.yaml (status codes each step must observe)
  </read_first>
  <action>
    Author scripts/smoke-auth.sh following check-contracts.sh house style verbatim (shebang, set -euo pipefail, ROOT via BASH_SOURCE, boxed purpose header, staged progress echoes). BASE defaults to ${AUTH_BASE_URL:-http://localhost:8081}. No jq exists on the host — use pure curl status-code extraction (-s -o /dev/null -w '%{http_code}') and a single sed expression to lift the accessToken value from the login body. Sequence with hard assertions: (1) signup with a random-suffix email expects 201; (2) immediate replay of the identical payload expects 409; (3) login with a wrong password expects 401; (4) login with correct credentials expects 200 and a non-empty extracted token; (5) GET /auth/me WITH the bearer token expects 200; (6) GET /auth/me WITHOUT any token expects 401; (7) POST to the logout route WITH the bearer token expects 404 or 405 — accepting either status proves the operation is absent per the frozen contract while tolerating framework routing differences. Exit non-zero on the first failed step with a labeled FAIL message; print an ALL-PASS verdict line at the end. Author with LF endings.

    Author services/auth-service/README.md containing: one-line service purpose; endpoint table listing exactly the three contracted operations plus an explicit statement that no server session-invalidation route exists because v1 logout is client-side token discard with no revocation (paraphrase the contract's info.description paragraph and reference it); consumed environment variables table (JWT_SECRET, JWT_ISSUER, JWT_AUDIENCE, JWT_TTL_SECONDS, POSTGRES_USER, POSTGRES_PASSWORD, SPRING_DATASOURCE_URL); memory posture note (MaxRAMPercentage=75 + mem_limit 512m pairing); how-to-run sections for tests (./mvnw test, Docker required for Testcontainers) and the smoke script; a residual-risk note that credential-stuffing throttling arrives with gateway rate limiting in Phase 7.
  </action>
  <verify>
    <automated>docker compose up -d --build && sleep 75 && bash scripts/smoke-auth.sh</automated>
  </verify>
  <acceptance_criteria>
    - Rebuild is REQUIRED before running: Wave 2 changed source after Plan 01 built the image, so verify starts with docker compose up -d --build and both services reach healthy
    - bash scripts/smoke-auth.sh exits 0 and prints its ALL-PASS verdict
    - The script contains zero jq invocations and runs under plain bash on Windows Git Bash
    - README states the client-side discard policy in a dedicated line referencing the contract decision
    - git ls-files --eol reports lf for both new files
  </acceptance_criteria>
  <done>AUTH-04 satisfied both ways: scripted runtime proof that the third write operation is absent, and committed documentation stating the discard policy.</done>
  <precondition>Docker daemon running; Wave 2 code merged so the rebuild carries final auth behavior.</precondition>
</task>

<task type="auto">
  <name>Task 2: Phase verification gate — full matrix green</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md Phase 2 success criteria 1-5 (this matrix maps 1:1 onto them)
    - scripts/check-contracts.sh Stage 5 (auth operationId assertions)
  </read_first>
  <action>
    Run the complete verification matrix and fix any red item at its source before re-running: (1) full Maven suite `cd services/auth-service && ./mvnw test` green including the Testcontainers slice; (2) `bash scripts/check-contracts.sh` passes confirming the auth spec still carries all three operationIds; (3) compose stack from Task 1 still shows both services healthy (`docker compose ps`); (4) re-run `bash scripts/smoke-auth.sh` against that same stack — ALL PASS; (5) LF audit: `git ls-files --eol | grep -i crlf` returns no rows among files added during Phases 1-2 auth work; (6) confirm `docker inspect` on the auth container still reports the 512m limit and that the transitional-port comment survives in docker-compose.yml. Record the matrix results in the plan summary.
  </action>
  <verify>
    <automated>cd services/auth-service && ./mvnw test && bash ../../scripts/check-contracts.sh && bash ../../scripts/smoke-auth.sh && git ls-files --eol | grep -ic crlf</automated>
  </verify>
  <acceptance_criteria>
    - All five commands exit 0 (the final grep count may legitimately be zero rows -> grep -c exits 1 when zero; treat empty crlf set as pass and assert explicitly)
    - Roadmap success criteria 1-5 each have a passing command or artifact backing them (map them in the summary)
  </acceptance_criteria>
  <done>The entire phase gate is demonstrably green: suite + contracts + live stack + smoke + hygiene, ready for /gsd-verify-work.</done>
  <precondition>Docker daemon running; Tasks/Plans 01-02 merged.</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke script -> :8081 | Verification traffic exercises the public boundary exactly as a real client would |
| Documentation -> operators | README shapes security expectations (residual risks, secret handling) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-stuff | Elevation | Credential stuffing residual risk | medium | accept | Documented in README as deferred to GTWY-05 (Phase 7); acceptance recorded here |
| T-02-docleak | Information Disclosure | Docs leaking secret material or token values | medium | mitigate | README documents env var NAMES only, never values; smoke script prints statuses and verdicts, never tokens |
| T-02-absence | Tampering | Someone later adding the forbidden third write operation | high | mitigate | Smoke script's absence assertion fails the phase gate permanently if the frozen contract is violated |
</threat_model>

<flagged_assumptions>
Spec-less probe edges: AUTH-04 (unclassified/unresolved in the fallback report) is closed HERE by two independent proofs — scripted runtime absence assertion plus documented policy statement. A6 flagged: sed-based token extraction is adequate for status-first scripting but brittle to response-format drift; acceptable now, replaceable if jq ever lands.
</flagged_assumptions>

<verification>
- `bash scripts/smoke-auth.sh` ALL PASS against a rebuilt, healthy stack
- `./mvnw test`, `check-contracts.sh`, LF audit all green simultaneously
- README documents the logout policy per AUTH-04
</verification>

<success_criteria>
Phase 2 closes with every roadmap success criterion backed by a runnable proof: signup/duplicate/hash (suite), login/JWT (suite), /me authorization (suite), documented client-side logout (README + absence assertion), and containerized health with standalone smoke (compose + script).
</success_criteria>

## Artifacts this phase produces

**New file paths:** scripts/smoke-auth.sh · services/auth-service/README.md
**Script surface:** smoke-auth.sh accepts AUTH_BASE_URL env override (defaults http://localhost:8081); exit 0 = ALL PASS, non-zero = labeled FAIL step
**Docs sections:** endpoint table (three contracted operations + explicit client-side-discard statement) · env-var table · memory posture · run instructions · residual-risk note (throttling deferred to Phase 7)
*(No new Java symbols, CLI flags, or compose keys — this plan closes the phase on Wave-2 surfaces.)*

<output>
Create `.planning/phases/02-auth-service/02-03-SUMMARY.md` when done
</output>
