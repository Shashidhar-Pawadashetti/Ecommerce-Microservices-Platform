---
phase: 02-auth-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/auth-service/pom.xml
  - services/auth-service/mvnw
  - services/auth-service/mvnw.cmd
  - services/auth-service/.mvn/wrapper/
  - services/auth-service/src/main/java/com/ecommerce/auth/AuthServiceApplication.java
  - services/auth-service/src/main/java/com/ecommerce/auth/config/SecurityConfig.java
  - services/auth-service/src/main/resources/application.yml
  - services/auth-service/src/main/resources/db/migration/V1__create_users.sql
  - services/auth-service/Dockerfile
  - docker-compose.yml
autonomous: true
requirements: [SC-5]
user_setup: []

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Multi-stage image builds (eclipse-temurin:21-jdk-alpine compile stage -> eclipse-temurin:21-jre-alpine non-root runtime stage) and runs as compose service auth-service"
    - "One postgres:18 container hosts the users database with a named volume mounted at /var/lib/postgresql (PG18 layout) and publishes no host port"
    - "From cold start, auth-service passes its compose healthcheck — proving Boot boot -> Flyway V1 migration on PG18 native dialect (uuid PK, text[] roles) -> actuator DataSource-health UP chain"
    - "Compose header documents the incremental-growth policy (D-01: Mongo Ph3, Redis Ph4, Kafka Ph5, Mailpit Ph6), replacing the stale Phase-1 text"
    - "The 8081 host port mapping carries an explicit transitional marker stating Phase 7 revokes it (D-09)"
    - "JVM memory posture pairs JAVA_TOOL_OPTIONS=-XX:MaxRAMPercentage=75 baked into the image with mem_limit: 512m on the compose entry (D-03)"
    - "./mvnw package produces a runnable jar from the committed wrapper with zero versions invented outside the parent BOM"
  artifacts:
    - services/auth-service/pom.xml
    - services/auth-service/mvnw
    - services/auth-service/src/main/resources/db/migration/V1__create_users.sql
    - services/auth-service/Dockerfile
    - docker-compose.yml (postgres + auth-service service keys)
    - services/auth-service/src/main/java/com/ecommerce/auth/AuthServiceApplication.java
    - services/auth-service/src/main/java/com/ecommerce/auth/config/SecurityConfig.java
    - services/auth-service/src/main/resources/application.yml
  key_links:
    - "compose auth-service depends_on postgres with condition: service_healthy"
    - "SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/users connects Boot -> Flyway V1 -> users table inside pgdata volume"
    - "JWT_SECRET / JWT_ISSUER / JWT_AUDIENCE / JWT_TTL_SECONDS env vars pass through compose into the container (consumed by Plan 02 beans)"
  prohibitions:
    - statement: "No logout endpoint exists in auth-service — logout is client-side token discard by frozen contract (OpenAPI info.description, D-03 lineage); adding one requires an explicit contract change."
      status: enforced-at-phase-close
      verification: "Runtime: POST to the logout route returns 404/405, asserted by scripts/smoke-auth.sh in Plan 02-03; Source: controller defines no such mapping."
    - statement: "Passwords are never persisted plaintext or reversibly encoded — only bcrypt hashes."
      status: enforced-at-phase-close
      verification: "Integration test (Plan 02-02) asserts the stored password_hash value starts with the bcrypt identifier prefix."
    - statement: "Hibernate ddl-auto stays validate while Flyway owns schema evolution; shipped V-named migrations are append-only and never edited after landing."
      status: enforced-at-phase-close
      verification: "Source: application.yml binds ddl-auto to validate; src/main/resources/db/migration contains only V1__create_users.sql at phase close."
---

<objective>
Stand up the first JVM service skeleton and prove the riskiest runtime family end-to-end BEFORE any auth features exist: a Spring Boot 3.5.16 Maven project (committed wrapper, parent-BOM-only versions), the Flyway V1 users migration, a multi-stage alpine Dockerfile, and docker-compose entries for postgres:18 + auth-service that come up healthy together. This plan IS the phase tracer slice — the template order-service and gateway copy later (per D-03/D-04/D-09 template-maximizing decisions).

Purpose: The phase goal names JVM-in-Docker as the riskiest ground (multi-stage build, memory flags, PG18 volume mount). Proving it in Wave 1 means an architectural dead-end costs one commit, not ten layers.

Output: Buildable Maven project; healthy 2-container compose stack with Flyway-migrated users schema; corrected compose growth-policy header.
</objective>

<execution_context>
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/workflows/execute-plan.md
@C:/Users/shash/OneDrive/Desktop/PROJECTS/Ecommerce-Microservices-Platform/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-service/02-CONTEXT.md
@.planning/phases/02-auth-service/02-RESEARCH.md
@.planning/phases/02-auth-service/02-PATTERNS.md
@docs/versions.md
@.env.example
@docker-compose.yml
@.gitattributes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Maven scaffold — Boot 3.5.16 project, committed wrapper, Flyway V1 migration, app shell</name>
  <files>services/auth-service/pom.xml, services/auth-service/mvnw, services/auth-service/mvnw.cmd, services/auth-service/.mvn/wrapper/, services/auth-service/src/main/java/com/ecommerce/auth/AuthServiceApplication.java, services/auth-service/src/main/java/com/ecommerce/auth/config/SecurityConfig.java, services/auth-service/src/main/resources/application.yml, services/auth-service/src/main/resources/db/migration/V1__create_users.sql</files>
  <read_first>
    - services/auth-service/ (confirm empty scaffold with .gitkeep only)
    - docs/versions.md (pinned versions are LAW — copy, never invent)
    - .planning/phases/02-auth-service/02-RESEARCH.md (Installation dependency block ~lines 118-136; Pattern 5 SQL half)
    - .planning/phases/02-auth-service/02-PATTERNS.md (line-ending law; no-analog notes)
    - .gitattributes (LF enforcement catch-all)
    - .planning/phases/02-auth-service/02-CONTEXT.md (decisions D-04, D-05, D-06, D-08)
  </read_first>
  <action>
    Obtain the Maven Wrapper WITHOUT assuming host Maven: if `command -v mvn` succeeds run `mvn -N org.apache.maven.plugins:maven-wrapper-plugin:wrapper` inside services/auth-service; otherwise download a Spring Initializr zip (`curl https://start.spring.io/starter.zip` with type=maven-project, language=java, javaVersion=21, groupId=com.ecommerce, artifactId=auth-service) and extract ONLY mvnw, mvnw.cmd, and .mvn/ into services/auth-service/ — discard the generated pom.xml and src/ because we author our own. Commit wrapper files; .gitattributes already forces LF on mvnw.

    Create pom.xml with parent org.springframework.boot:spring-boot-starter-parent at exactly 3.5.16 (version copied from docs/versions.md — no other version literal appears anywhere), groupId com.ecommerce, artifactId auth-service, property java.version=21. This targets release-21 bytecode; the host JDK 23.0.1 compiles it correctly (research A4 resolution of D-08's JDK-21 prerequisite — bytecode and container runtime are 21 regardless of host major). Declare exactly these dependencies, all versionless (parent BOM manages them): starters web, security, oauth2-resource-server, data-jpa, validation, actuator; org.flywaydb:flyway-core AND org.flywaydb:flyway-database-postgresql (both coordinates REQUIRED since the Flyway 10 module split — missing the postgresql companion crashes boot with "No database found to handle jdbc:postgresql"); org.postgresql:postgresql runtime scope; test scope: starter-test, spring-boot-testcontainers, org.testcontainers:postgresql, org.testcontainers:junit-jupiter. Add spring-boot-maven-plugin.

    Create src/main/resources/application.yml: server.port 8081; spring.datasource.url bound as ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/users}, username ${POSTGRES_USER:ecommerce}, password ${POSTGRES_PASSWORD:}; spring.jpa.hibernate.ddl-auto: validate (Flyway owns the schema — validate catches entity/migration drift); management.endpoints.web.exposure.include: health (minimal exposure per research Pitfall 5). Two-space indent per .editorconfig YAML convention.

    Create com.ecommerce.auth.AuthServiceApplication (standard @SpringBootApplication main). Create com.ecommerce.auth.config.SecurityConfig with a minimal SecurityFilterChain bean for THIS slice only: CSRF disabled, SessionCreationPolicy.STATELESS, permitAll for the actuator health path, anyRequest().denyAll(). No resource-server customizer yet (lands in Plan 02 Tasks 2-3); the starter on the classpath is inert without it.

    Create src/main/resources/db/migration/V1__create_users.sql implementing D-04/D-05/D-06 exactly: table users with id uuid PRIMARY KEY (app-generated UUID per D-05), email varchar(255) NOT NULL, password_hash varchar(255) NOT NULL, roles text[] NOT NULL DEFAULT '{customer}' (native array per D-06), created_at timestamptz NOT NULL DEFAULT now(); then CREATE UNIQUE INDEX users_email_uniq ON users (email) — the index is the atomic duplicate guard (no select-then-insert anywhere, ever). Header comment states: owned by Flyway, append-only, never edited after landing; order-service copies this pattern in Phase 5.
  </action>
  <verify>
    <automated>cd services/auth-service && ./mvnw -q -B -DskipTests package && ls target/*.jar</automated>
  </verify>
  <acceptance_criteria>
    - ./mvnw -B -DskipTests package exits 0 and target/ contains exactly one executable jar
    - pom.xml parent is spring-boot-starter-parent 3.5.16 and contains flyway-database-postgresql alongside flyway-core
    - No version element appears on any dependency declaration (parent-BOM-only versions)
    - V1__create_users.sql creates table users with id uuid PRIMARY KEY, email varchar(255), password_hash varchar(255), roles text[] defaulting to the single-element customer array, created_at timestamptz, plus unique index users_email_uniq on email
    - application.yml binds ddl-auto to the validate value on an uncommented line and management.endpoints.web.exposure.include covers health
    - git ls-files --eol reports lf for mvnw and every new text file
  </acceptance_criteria>
  <done>Maven project builds a runnable jar via committed wrapper with release-21 targeting; Flyway V1 migration encodes the D-05/D-06 schema exactly; zero version pins outside the parent BOM.</done>
  <precondition>A local JDK (17+) is on PATH so ./mvnw can launch; no Maven install required.</precondition>
</task>

<task type="tracer">
  <name>Task 2 (TRACER): JVM-in-Docker proof — image, compose pair, cold-start healthy with Flyway-migrated PG18</name>
  <files>services/auth-service/Dockerfile, docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (current 14-line header — rewrite lines 4-9, then append service keys)
    - .env.example (contractual variable NAMES: JWT_SECRET, JWT_ISSUER, JWT_AUDIENCE, JWT_TTL_SECONDS, POSTGRES_USER, POSTGRES_PASSWORD — add nothing new)
    - .planning/phases/02-auth-service/02-RESEARCH.md (Pattern 7 Dockerfile, Pattern 8 compose, Pitfalls 5/6/8, Environment Availability)
    - docs/versions.md (temurin 21-jre base images — alpine/noble variants; Pattern 7 selects alpine)
    - .planning/phases/02-auth-service/02-CONTEXT.md (decisions D-01, D-02, D-03, D-09)
  </read_first>
  <action>
    First ensure a local .env exists: cp .env.example .env (gitignored), regenerate JWT_SECRET with `openssl rand -base64 32` (CSPRNG, >=32 decoded bytes per json-interop.md Secret Handling) and set a real POSTGRES_PASSWORD. Agent-doable local setup, not user setup.

    Create services/auth-service/Dockerfile as a production-quality multi-stage build per research Pattern 7 (this is the copied template for order-service/gateway): build stage FROM eclipse-temurin:21-jdk-alpine, WORKDIR /workspace, COPY .mvn/ .mvn/, COPY mvnw pom.xml ./, RUN ./mvnw -q -B dependency:go-offline (best-effort layer cache — tolerate failure with || true so offline quirks never break the build), COPY src ./src, RUN ./mvnw -q -B package -DskipTests; runtime stage FROM eclipse-temurin:21-jre-alpine, create and switch to non-root user spring (addgroup/adduser), WORKDIR /app, COPY --from=build the target jar as app.jar, ENV JAVA_TOOL_OPTIONS="-XX:MaxRAMPercentage=75" (D-03 flag posture), EXPOSE 8081, ENTRYPOINT ["java","-jar","/app/app.jar"].

    Rewrite the docker-compose.yml header (lines 4-9 currently claim Phase 2 adds Kafka/Mongo/Redis/Mailpit/Kafka-UI — stale per D-01) to state the incremental-growth policy: Phase 2 adds postgres:18 + auth-service only; Mongo joins in Phase 3, Redis in Phase 4, Kafka in Phase 5, Mailpit in Phase 6 — each datastore/broker arrives with its first consuming service; Phase 9 completes healthcheck/memory/smoke wiring. Keep the boxed-comment convention and the "values come from .env" line.

    Append two service keys per research Pattern 8 with one deliberate correction. postgres: image postgres:18; environment POSTGRES_USER/POSTGRES_PASSWORD interpolated from .env and POSTGRES_DB: users — ONLY users now; D-02 honored literally: the orders database is added by order-service in Phase 5, NOT pre-created here. Record in a comment beside this key the Phase 5 obligation: postgres init scripts under /docker-entrypoint-initdb.d execute solely during first initialization of an empty data volume, so Phase 5 must ship both an init-script backfill AND a one-time CREATE DATABASE for long-lived volumes. volumes: pgdata:/var/lib/postgresql — mount the ROOT path (the PG18 declared VOLUME); mounting the old /data suffix silently misplaces data (versions.md pin). healthcheck: CMD-SHELL pg_isready against the users database with interval 10s, timeout 5s, retries 5. Publish NO host ports (network-internal, GTWY-04 direction). auth-service: build ./services/auth-service; ports "8081:8081" preceded by an explicit comment marking it TRANSITIONAL for Phases 2-6 and revoked in Phase 7 (D-09); mem_limit: 512m (D-03 pairing); environment keys: SPRING_DATASOURCE_URL jdbc:postgresql://postgres:5432/users, SPRING_DATASOURCE_USERNAME ${POSTGRES_USER}, SPRING_DATASOURCE_PASSWORD ${POSTGRES_PASSWORD}, JWT_SECRET/JWT_ISSUER/JWT_AUDIENCE/JWT_TTL_SECONDS passthrough from .env, SERVER_PORT "8081". CORRECTION vs research Pattern 8 draft: use the single-word SPRING_DATASOURCE_* spellings — Boot relaxed binding maps SPRING_DATASOURCE_URL to spring.datasource.url, whereas the draft's split spelling binds to a nonexistent property and the datasource would silently fall back to the localhost default inside the container. depends_on: postgres with condition service_healthy. healthcheck: CMD-SHELL wget -qO- http://localhost:8081/actuator/health piped through grep for UP with exit 1 on miss (BusyBox wget ships on alpine; curl does not — Pitfall 5), interval 15s, timeout 5s, retries 5, start_period 60s (generous for JVM boot + Flyway migrate). Declare the pgdata named volume.
  </action>
  <verify>
    <automated>docker compose up -d --build && sleep 75 && curl -fsS http://localhost:8081/actuator/health | grep -o UP && docker compose exec -T postgres psql -U "${POSTGRES_USER:-ecommerce}" -d users -c "\dt" | grep -E "users|flyway_schema_history" && docker compose down</automated>
  </verify>
  <acceptance_criteria>
    - docker compose config validates with exactly the services postgres and auth-service present
    - docker compose up -d --build brings both containers to health Status=healthy within the start_period budget (~90s)
    - GET http://localhost:8081/actuator/health returns 200 whose body contains status UP (UP implies DataSource reachable AND Flyway V1 applied — Boot fails startup otherwise)
    - psql table listing inside the postgres container shows both flyway_schema_history and users in database users
    - The 8081 ports entry sits adjacent to its transitional/revoke-in-Phase-7 comment; the postgres service has no published ports
    - docker inspect of the auth container reports Memory equal to 536870912 bytes (512m mem_limit effective under Compose v2 — research A2 check)
    - Compose header contains the incremental-growth policy wording naming Mongo Phase 3 / Redis Phase 4 / Kafka Phase 5 / Mailpit Phase 6
  </acceptance_criteria>
  <done>The tracer path is proven end-to-end and committed: HTTP request on :8081 -> SecurityFilterChain -> actuator health -> JDBC -> postgres:18 with the Flyway-migrated users schema, from a cold `docker compose up`, both containers healthy.</done>
  <precondition>Docker Desktop daemon is RUNNING (`docker info` exits 0) — it was down at research time and is a hard blocker for this task; start it before executing.</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host -> auth-service (:8081) | Public dev-machine ingress during Phases 2-6; untrusted HTTP enters here |
| auth-service -> postgres | Service-to-datastore inside the Compose network; DB publishes no host port |
| .env -> containers | Secrets enter via env interpolation only; never baked into image layers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | Maven coordinates in pom.xml | high | mitigate | Package Legitimacy Audit complete in 02-RESEARCH.md — all coordinates registry-verified OK, zero ASSUMED/SUS/SLOP verdicts; parent-BOM-only versions prevent drift; no legitimacy checkpoint required |
| T-02-08 | DoS | Unthrottled auth routes on :8081 | medium | accept | Rate limiting is GTWY-05 (Phase 7 gateway); v1 residual risk documented in service README (Plan 02-03) |
| T-02-vol | Tampering | PG18 volume mis-mount silently losing data | medium | mitigate | Named volume mounted at /var/lib/postgresql root per versions.md pin; verify asserts users + flyway_schema_history exist post-up |
| T-02-root | Elevation | Root-running container | medium | mitigate | Runtime stage switches to non-root user spring before ENTRYPOINT |
| T-02-dbex | Information Disclosure | Postgres exposed to host network | medium | mitigate | No ports key on postgres service; reachable only inside the Compose network |
</threat_model>

<flagged_assumptions>
Spec-less probe fallback: requirement edges AUTH-01..AUTH-04 returned unclassified/unresolved (no SPEC.md for this phase). They are NOT silently dropped and NOT auto-promoted — each is planned explicitly from ROADMAP success criteria + RESEARCH.md and lands as: AUTH-01/02/03 implemented + test-verified in Plan 02 Tasks 1-3; AUTH-04 in Plan 02-03. Additional flagged assumptions carried by this plan:
- A4: host JDK is 23.0.1, not D-08's literal 21 — resolved via java.version=21 release targeting (bytecode/container are 21); installing Temurin 21 stays optional for strictness.
- A2: mem_limit (not deploy.resources.limits) assumed effective under Compose v2 — verified empirically via docker inspect in Task 2 acceptance criteria.
- A5: BusyBox wget on 21-jre-alpine supports the -qO- healthcheck form — falsified instantly by an unhealthy container; fallback form is wget -q -O-.
- D-02 literal reading (orders DB deferred to Phase 5) vs research recommendation to pre-create it now — LOCKED DECISION WINS; Pitfall-8 mitigation obligation recorded in the compose comment for Phase 5 planning.
</flagged_assumptions>

<verification>
- `cd services/auth-service && ./mvnw -B -DskipTests package` exits 0
- `docker compose up -d --build` -> both services healthy; `/actuator/health` UP; `users` and `flyway_schema_history` tables exist; `docker compose down` cleans up
- `bash scripts/check-contracts.sh` still passes (no contract files touched)
- `git ls-files --eol` shows lf for every file this plan added
</verification>

<success_criteria>
The phase's riskiest runtime family is proven and templated: a fresh clone on Windows can run `docker compose up -d --build` and reach a healthy auth-service whose health reflects a successfully Flyway-migrated postgres:18 — using only committed files plus a locally generated .env.
</success_criteria>

<output>
Create `.planning/phases/02-auth-service/02-01-SUMMARY.md` when done
</output>
