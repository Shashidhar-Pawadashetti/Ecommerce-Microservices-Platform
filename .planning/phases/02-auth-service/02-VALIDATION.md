---
phase: 2
slug: auth-service
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-25
---

# Phase 2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 5 + AssertJ + Spring Boot Test (Boot 3.5.16-managed) + Testcontainers 1.21.x (`postgres:18`) |
| **Config file** | none — Wave 0 installs via `pom.xml` test dependencies (Plan 02-01 Task 1) |
| **Quick run command** | `cd services/auth-service && ./mvnw -q test -Dtest=UserServiceTests` (no Docker needed) |
| **Full suite command** | `cd services/auth-service && ./mvnw test` (includes Testcontainers slice — requires Docker daemon) |
| **Estimated runtime** | ~60–120 s full suite (first Testcontainers image pull may add minutes once) |

---

## Sampling Rate

- **After every task commit:** Run `./mvnw -q test -Dtest=UserServiceTests` (or the task's targeted `-Dtest=` slice)
- **After every plan wave:** Run `./mvnw test` full suite (Testcontainers slice green)
- **Before `/gsd-verify-work`:** Full suite green AND `bash scripts/check-contracts.sh` passes AND `bash scripts/smoke-auth.sh` prints its ALL-PASS verdict against a rebuilt healthy stack
- **Max feedback latency:** ~120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02-01-T1 | 01 | 1 | SC-5 (infra) | T-02-SC | Parent-BOM-only pinned coordinates; committed LF wrapper | build | `./mvnw -B -DskipTests package` | ❌ W0 (task creates) | ⬜ pending |
| 02-01-T2 | 01 | 1 | SC-5 | T-02-vol / T-02-root / T-02-dbex | Non-root runtime; PG publishes no host port; volume at PG18 root; healthcheck UP implies Flyway applied | container e2e | `docker compose up -d --build` + health curl + psql `\dt` | ❌ W0 | ⬜ pending |
| 02-02-T1 | 02 | 2 | AUTH-01 | T-02-mass / T-02-sqli | Lowercase-normalized signup; app-generated UUIDs; bcrypt-only credential material; fast conflict signal | unit | `./mvnw test -Dtest='UserServiceTests'` | ❌ W0 (task creates) | ⬜ pending |
| 02-02-T2 | 02 | 2 | AUTH-01 | T-02-mass | Signup HTTP contract: 201 ms-Z shape; duplicate→409 exact body; validation→400 exact body; stored-hash assertion | integration (Testcontainers) | `./mvnw test -Dtest='AuthFlowIntegrationTests'` | ❌ W0 (task creates) | ⬜ pending |
| 02-03-T1 | 03 | 3 | AUTH-02 | T-02-01 / T-02-02 / T-02-04 / T-02-05 | HS256-pinned issuance; byte-identical anti-enumeration 401s; ≥32-byte secret startup assertion; length-only logging | integration | `./mvnw test -Dtest='AuthFlowIntegrationTests'` | ✅ (extended in-task) | ⬜ pending |
| 02-03-T2 | 03 | 3 | AUTH-03 | T-02-01 | `/me` authz across 7 positive/negative cases incl. expired(+61s)/foreign-signed/wrong-alg | integration | `./mvnw test` | ✅ (extended in-task) | ⬜ pending |
| 02-04-T1 | 04 | 4 | AUTH-04 | T-02-absence | Runtime proof the forbidden third write op is absent (404/405); README policy statement | smoke script | `bash scripts/smoke-auth.sh` | ❌ W0 (task creates) | ⬜ pending |
| 02-04-T2 | 04 | 4 | AUTH-04 + SC-5 | — | Full gate matrix: suite + contracts + healthy stack + smoke + LF audit (explicit count==0 assertion) | gate | `./mvnw test && ../../scripts/check-contracts.sh && ../../scripts/smoke-auth.sh && [ "$(git ls-files --eol \| grep -i crlf \| wc -l)" -eq 0 ]` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `services/auth-service/pom.xml` with test-scope dependencies (starter-test, spring-boot-testcontainers, testcontainers postgresql + junit-jupiter) — Plan 02-01 Task 1
- [ ] Maven wrapper committed (LF) so tests run without host Maven — Plan 02-01 Task 1
- [ ] Local `.env` generated by executor: `JWT_SECRET=$(openssl rand -base64 32)` (CSPRNG ≥32 decoded bytes per json-interop §Secret Handling) — agent-doable, not user setup
- [ ] Docker Desktop daemon running before any Testcontainers/compose task (hard blocker flagged as precondition in Plans 01–04)

---

## Manual-Only Verifications

All phase behaviors have automated verification. (Container health/mem-limit checks are scripted via compose/curl/docker-inspect commands embedded in plan verify blocks.)

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120 s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
